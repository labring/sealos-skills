---
phase: 04-install-smoke-and-handoff
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-package-audit.json
  - .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.stderr.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.exitcode
  - .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-discovery.json
  - .planning/phases/04-install-smoke-and-handoff/evidence/07-validator-and-json-checks.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/08-milestone-files.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/08-working-tree-status.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/08-skills-files.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/08-phase-4-uncommitted-files.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md
  - .planning/phases/04-install-smoke-and-handoff/evidence/10-compat-smoke-env.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json
  - .planning/phases/04-install-smoke-and-handoff/evidence/12-secret-scan.txt
  - .planning/phases/04-install-smoke-and-handoff/evidence/13-cleanup-verification.txt
  - .planning/phases/04-install-smoke-and-handoff/04-02-SUMMARY.md
autonomous: false
requirements:
  - HAND-02
  - HAND-03
user_setup:
  - service: npm package legitimacy checkpoint
    why: "The Phase 4 research marks npm package `plugins` as [ASSUMED], so compatibility install evidence uses a blocking human package legitimacy gate before executing `npx plugins add`."
    dashboard_config:
      - task: "Verify `plugins` package identity and provenance at npmjs.com/package/plugins before approving the compatibility install attempt."
        location: "https://www.npmjs.com/package/plugins"
must_haves:
  truths:
    - "HAND-02/D-09/D-11/D-12: Compatibility evidence records the exact `npx plugins add https://github.com/labring/sealos-skills --target codex` command, isolated env paths, exit code, stdout, stderr, and any available plugin identity fields."
    - "HAND-02/D-10/D-21: If full compatibility install is blocked by package, network, or host behavior, discovery evidence is captured and explicitly labeled as compatibility discovery evidence."
    - "D-03: Final handoff scope stays on install and distribution surfaces; deploy, database, S3, canvas, and app-builder runtime behavior remain out of scope."
    - "HAND-03/D-13/D-14/D-15/D-16: Final verification records smoke env paths, cleanup status, scoped generated evidence, validator output, JSON syntax checks, and `git diff -- skills --exit-code`."
    - "HAND-03/D-17/D-18/D-19: Final handoff lists native evidence, compatibility evidence, validator checks, exact milestone changed files, Phase 4 source implementation changes, `skills/**` milestone changes, uncommitted working-tree state, and remaining non-Codex follow-up."
    - "HAND-03/D-22: Evidence is scanned before commit for tokens, kubeconfig data, auth files, npm tokens, API keys, and user secrets."
  artifacts:
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-package-audit.json"
      provides: "Package metadata evidence for the `plugins` npm package"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.txt"
      provides: "Compatibility installer stdout evidence"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.stderr.txt"
      provides: "Compatibility installer stderr evidence"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.exitcode"
      provides: "Compatibility installer exit code"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-discovery.json"
      provides: "Fallback compatibility discovery evidence when full install is blocked"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/07-validator-and-json-checks.txt"
      provides: "Validator and JSON syntax transcript"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md"
      provides: "Final changed-file and follow-up handoff"
    - path: ".planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json"
      provides: "Aggregate HAND-01/HAND-02/HAND-03 assertion JSON"
    - path: ".planning/phases/04-install-smoke-and-handoff/04-02-SUMMARY.md"
      provides: "Execution summary for compatibility evidence and final handoff"
    - path: ".planning/phases/04-install-smoke-and-handoff/04-VERIFICATION.md"
      provides: "Post-execution verifier artifact that must cite evidence files"
    - path: ".planning/phases/04-install-smoke-and-handoff/04-UAT.md"
      provides: "Post-execution UAT artifact that must cite evidence files"
  key_links:
    - from: "README.md"
      to: ".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.txt"
      via: "exact compatibility command evidence"
      pattern: "npx plugins add https://github.com/labring/sealos-skills --target codex"
    - from: ".planning/phases/04-install-smoke-and-handoff/evidence/06-native-smoke-assertions.json"
      to: ".planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json"
      via: "aggregate assertion import"
      pattern: "native"
    - from: "git diff --name-only"
      to: ".planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md"
      via: "milestone base and working-tree truth"
      pattern: "milestone_base"
---

<objective>
Capture compatibility `npx plugins` evidence with a package legitimacy gate, then generate aggregate install assertions, validation transcripts, cleanup proof, secret scan, and the final changed-file handoff.

Purpose: Satisfy HAND-02 and HAND-03 while keeping Phase 4 focused on install/distribution evidence and source implementation stability.
Output: Compatibility evidence or labeled discovery fallback, aggregate assertion JSON, validator/JSON/skills checks, final handoff, and `04-02-SUMMARY.md`.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-install-smoke-and-handoff/04-CONTEXT.md
@.planning/phases/04-install-smoke-and-handoff/04-RESEARCH.md
@.planning/phases/04-install-smoke-and-handoff/04-01-SUMMARY.md
@.planning/phases/04-install-smoke-and-handoff/evidence/06-native-smoke-assertions.json
@.planning/phases/02-readme-and-metadata-alignment/02-VERIFICATION.md
@.planning/phases/03-validator-hardening/03-VERIFICATION.md
@README.md
@scripts/validate-codex-plugin.py
@distribution/platforms.json
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 4 | Maintainers finish with native install evidence, compatibility evidence, and changed-file handoff. | 04-01, 04-02 | COVERED | This plan completes compatibility and handoff after native evidence exists. |
| REQ | HAND-01 | Native Codex smoke output. | 04-01 | COVERED | Dependency provides native evidence and assertion JSON. |
| REQ | HAND-02 | Compatibility install or discovery evidence for the npx path. | 04-02 | COVERED | Tasks 1-2 cover package audit, install attempt, and fallback discovery evidence. |
| REQ | HAND-03 | Exact files changed and non-Codex follow-up. | 04-02 | COVERED | Task 3 generates handoff from git truth. |
| RESEARCH | Package legitimacy audit | `plugins` npm package is `[ASSUMED]`; checkpoint before install. | 04-02 | COVERED | Task 1 is a blocking-human checkpoint. |
| RESEARCH | Compatibility evidence fallback | Full install preferred; discovery/help/version evidence acceptable when full install is blocked. | 04-02 | COVERED | Task 2 implements D-09/D-10 strategy. |
| RESEARCH | Final validation | Run validator, JSON checks, skills diff, secret scan, cleanup, and git handoff. | 04-02 | COVERED | Task 3 implements final verification and handoff. |
| CONTEXT | D-01, D-02, D-03 | Evidence-first closeout, fresh proof, install/distribution scope. | 04-01, 04-02 | COVERED | This plan aggregates evidence and reports implementation-source stability. |
| CONTEXT | D-09, D-10, D-11, D-12 | Compatibility install or discovery evidence, isolation, command, exit code, stdout/stderr. | 04-02 | COVERED | Tasks 1-2 cite and implement compatibility requirements. |
| CONTEXT | D-13, D-14, D-15, D-16 | Env evidence, cleanup, scoped generated files, validator/JSON/skills checks. | 04-02 | COVERED | Tasks 2-3 record env, cleanup, validation, and scope. |
| CONTEXT | D-17, D-18, D-19 | Final handoff content, git truth base, committed/uncommitted split. | 04-02 | COVERED | Task 3 generates handoff from git commands. |
| CONTEXT | D-20, D-21, D-22 | Remote follow-up, npx variability, secret-free evidence. | 04-02 | COVERED | Task 3 labels follow-up/fallback and scans evidence. |
</source_audit>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `plugins` npm package legitimacy before compatibility install</name>
  <files>.planning/phases/04-install-smoke-and-handoff/evidence/05-npx-package-audit.json</files>
  <action>Capture package metadata before any compatibility install attempt because Phase 4 research marks `plugins` as `[ASSUMED]` and package legitimacy checkpoints are blocking for npm install tasks. Write `05-npx-package-audit.json` with the command `npm view plugins name version repository.url homepage bin engines scripts.postinstall --json`, the current `node --version`, `npm --version`, `npx --version`, and the package audit disposition. Then pause for human verification at `https://www.npmjs.com/package/plugins`, confirming package name `plugins`, repository `github.com/vercel-labs/plugins`, version shown by npm metadata, CLI bin, and no unexpected install script for the package version under test.</action>
  <verify>
    <automated>test -s .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-package-audit.json && python3 -m json.tool .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-package-audit.json >/dev/null</automated>
    <human-check>Open https://www.npmjs.com/package/plugins and approve only after the package identity and provenance match `05-npx-package-audit.json`.</human-check>
  </verify>
  <done>Package legitimacy evidence exists and the human gate approves the compatibility install attempt, satisfying the package legitimacy gate for the `[ASSUMED]` npm package.</done>
</task>

<task type="auto">
  <name>Task 2: Capture compatibility install or labeled discovery evidence</name>
  <files>.planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.txt, .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.stderr.txt, .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.exitcode, .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-discovery.json, .planning/phases/04-install-smoke-and-handoff/evidence/10-compat-smoke-env.txt</files>
  <action>After Task 1 approval, create isolated compatibility state per D-11: `NPX_HOME="$(mktemp -d)"`, `NPX_CACHE="$NPX_HOME/.npm-cache"`, `NPX_CODEX_HOME="$NPX_HOME/.codex"`, `XDG_CACHE_HOME="$NPX_HOME/.cache"`, and `XDG_CONFIG_HOME="$NPX_HOME/.config"`. Record all paths and the exact command in `10-compat-smoke-env.txt` per D-12/D-13. Run the documented compatibility command per D-09: `HOME="$NPX_HOME" CODEX_HOME="$NPX_CODEX_HOME" npm_config_cache="$NPX_CACHE" XDG_CACHE_HOME="$XDG_CACHE_HOME" XDG_CONFIG_HOME="$XDG_CONFIG_HOME" npx plugins add https://github.com/labring/sealos-skills --target codex`, writing stdout to `05-npx-compat-install.txt`, stderr to `05-npx-compat-install.stderr.txt`, and the numeric exit code to `05-npx-compat-install.exitcode`. If the command exits non-zero because of package, network, or host behavior, capture D-10 fallback evidence in `05-npx-compat-discovery.json`: `npx --version`, `npm view plugins name version repository.url homepage bin engines scripts.postinstall --json`, README exact fallback command proof, validator proof for the fallback command, exit code, and a `status: "discovery_evidence"` classification. If full install succeeds, create `05-npx-compat-discovery.json` with `status: "full_install_passed"` and any installed/discovered plugin identity fields available from stdout or written state. Preserve truthful labeling per D-21.</action>
  <verify>
    <automated>test -s .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.exitcode && test -f .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.txt && test -f .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-install.stderr.txt && python3 -m json.tool .planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-discovery.json >/dev/null</automated>
    <automated>python3 -c 'import json, pathlib; d=json.loads(pathlib.Path(".planning/phases/04-install-smoke-and-handoff/evidence/05-npx-compat-discovery.json").read_text()); assert d["status"] in ("full_install_passed", "discovery_evidence"); assert "npx plugins add https://github.com/labring/sealos-skills --target codex" in d["command"]'</automated>
  </verify>
  <done>HAND-02 is supported by full compatibility install evidence or explicitly labeled compatibility discovery evidence, with stdout, stderr, exit code, command, environment paths, and package metadata recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Generate aggregate assertions, cleanup proof, validation transcript, and final handoff</name>
  <files>.planning/phases/04-install-smoke-and-handoff/evidence/07-validator-and-json-checks.txt, .planning/phases/04-install-smoke-and-handoff/evidence/08-milestone-files.txt, .planning/phases/04-install-smoke-and-handoff/evidence/08-working-tree-status.txt, .planning/phases/04-install-smoke-and-handoff/evidence/08-skills-files.txt, .planning/phases/04-install-smoke-and-handoff/evidence/08-phase-4-uncommitted-files.txt, .planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md, .planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json, .planning/phases/04-install-smoke-and-handoff/evidence/12-secret-scan.txt, .planning/phases/04-install-smoke-and-handoff/evidence/13-cleanup-verification.txt, .planning/phases/04-install-smoke-and-handoff/04-02-SUMMARY.md</files>
  <action>Per D-16, write `07-validator-and-json-checks.txt` with command transcripts for `python3 scripts/validate-codex-plugin.py`, JSON syntax checks for `.codex-plugin/plugin.json`, `plugin.json`, `.agents/plugins/marketplace.json`, `marketplace.json`, `distribution/platforms.json`, and `git diff -- skills --exit-code`. Per D-18, choose `MILESTONE_BASE="$(git merge-base HEAD upstream/main)"`; if upstream is unavailable, use the first milestone commit parent and state the chosen base explicitly. Generate `08-milestone-files.txt` from `git diff --name-only "$MILESTONE_BASE"..HEAD | sort`, `08-working-tree-status.txt` from `git status --short`, `08-skills-files.txt` from `git diff --name-only "$MILESTONE_BASE"..HEAD -- skills | sort`, and `08-phase-4-uncommitted-files.txt` from `git diff --name-only HEAD -- .planning/phases/04-install-smoke-and-handoff | sort`. Per D-17 and D-19, write `08-final-handoff.md` with native evidence files from Plan 04-01, compatibility evidence files from this plan, validator/JSON results, exact changed files grouped by source, metadata, docs, planning, and evidence, exact Phase 4 source implementation changes, exact milestone `skills/**` changes, current uncommitted status, and remaining non-Codex follow-up: distribution-wide validator, CI or documented all-distribution command, and non-Codex screenshot/GIF refresh. Per D-22, scan Phase 4 evidence before commit for tokens, kubeconfig data, auth files, npm tokens, API keys, and user secrets; write `12-secret-scan.txt` with `status: clean` only after review. Remove compatibility temp homes when no longer needed and write `13-cleanup-verification.txt` with native and compatibility cleanup status per D-14. Create aggregate `11-install-smoke-assertions.json` importing `06-native-smoke-assertions.json`, `05-npx-compat-discovery.json`, validation status, cleanup status, secret-scan status, and handoff status; set top-level `passed: true` only when HAND-01, HAND-02, and HAND-03 evidence is complete. Create `04-02-SUMMARY.md` from the execution summary template. Name downstream closeout artifacts `04-VERIFICATION.md` and `04-UAT.md` in `08-final-handoff.md` so verifier and UAT workflows cite the same evidence set.</action>
  <verify>
    <automated>python3 scripts/validate-codex-plugin.py && python3 -m json.tool .codex-plugin/plugin.json >/dev/null && python3 -m json.tool plugin.json >/dev/null && python3 -m json.tool .agents/plugins/marketplace.json >/dev/null && python3 -m json.tool marketplace.json >/dev/null && python3 -m json.tool distribution/platforms.json >/dev/null</automated>
    <automated>python3 -m json.tool .planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json >/dev/null && python3 -c 'import json, pathlib; d=json.loads(pathlib.Path(".planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json").read_text()); assert d["passed"] is True; assert d["native"]["passed"] is True; assert d["compatibility"]["status"] in ("full_install_passed", "discovery_evidence"); assert d["handoff"]["present"] is True'</automated>
    <automated>git diff -- skills --exit-code</automated>
    <automated>test -s .planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md && rg -n 'HAND-01|HAND-02|HAND-03|milestone_base|remaining non-Codex distribution follow-up|04-VERIFICATION.md|04-UAT.md' .planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md</automated>
    <automated>rg -n '^status: clean$' .planning/phases/04-install-smoke-and-handoff/evidence/12-secret-scan.txt && rg -n 'cleanup_status' .planning/phases/04-install-smoke-and-handoff/evidence/13-cleanup-verification.txt</automated>
  </verify>
  <done>HAND-03 is satisfied by a git-truth handoff, aggregate assertions pass for HAND-01/HAND-02/HAND-03, validator/JSON/skills checks pass, evidence secret scan is clean, cleanup proof is recorded, and downstream `04-VERIFICATION.md` plus `04-UAT.md` artifact names are fixed for closeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local compatibility smoke | `npx plugins` compatibility evidence may download and execute npm package code. |
| Compatibility smoke to temp filesystem | Package-manager and plugin installer state must stay under isolated temp directories. |
| Git history to final handoff | Handoff claims must derive from git diff/status truth. |
| Evidence to committed repository | Captured command output must be secret-free before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | `npx plugins` compatibility command | mitigate | Capture package metadata, human-verify npm package identity, and record the exact command per D-09/D-12. |
| T-04-08 | Tampering | Maintainer Codex/npm state | mitigate | Run compatibility evidence with temp HOME, CODEX_HOME, npm cache, XDG_CACHE_HOME, and XDG_CONFIG_HOME per D-11. |
| T-04-09 | Repudiation | Compatibility evidence classification | mitigate | Record stdout, stderr, exit code, package metadata, and explicit `full_install_passed` or `discovery_evidence` status per D-10/D-21. |
| T-04-10 | Information Disclosure | Phase 4 evidence files | mitigate | Scan evidence for tokens, kubeconfig data, auth files, npm tokens, API keys, and user secrets before commit per D-22. |
| T-04-11 | Denial of Service | Temp directory accumulation | mitigate | Remove compatibility temp roots and record cleanup in `13-cleanup-verification.txt` per D-14. |
| T-04-12 | Tampering | Final changed-file handoff | mitigate | Generate changed-file lists from `git merge-base`, `git diff --name-only`, and `git status --short` per D-18/D-19. |
| T-04-SC | Tampering | npm package install | mitigate | Blocking human package legitimacy checkpoint for `[ASSUMED]` npm package `plugins`, followed by isolated temp-state execution. |
</threat_model>

<verification>
- `python3 scripts/validate-codex-plugin.py`
- `python3 -m json.tool .codex-plugin/plugin.json >/dev/null`
- `python3 -m json.tool plugin.json >/dev/null`
- `python3 -m json.tool .agents/plugins/marketplace.json >/dev/null`
- `python3 -m json.tool marketplace.json >/dev/null`
- `python3 -m json.tool distribution/platforms.json >/dev/null`
- `python3 -m json.tool .planning/phases/04-install-smoke-and-handoff/evidence/11-install-smoke-assertions.json >/dev/null`
- `git diff -- skills --exit-code`
- `rg -n '^status: clean$' .planning/phases/04-install-smoke-and-handoff/evidence/12-secret-scan.txt`
- `rg -n 'HAND-01|HAND-02|HAND-03|04-VERIFICATION.md|04-UAT.md' .planning/phases/04-install-smoke-and-handoff/evidence/08-final-handoff.md`
</verification>

<success_criteria>
HAND-02 and HAND-03 are satisfied when compatibility install or discovery evidence is truthfully labeled, aggregate assertions pass, validator and JSON checks pass, `skills/**` remains stable, evidence cleanup and secret scan are recorded, and the final handoff lists exact milestone files plus remaining non-Codex follow-up.
</success_criteria>

<output>
Create `.planning/phases/04-install-smoke-and-handoff/04-02-SUMMARY.md` when done. Downstream phase closeout creates `.planning/phases/04-install-smoke-and-handoff/04-VERIFICATION.md` and `.planning/phases/04-install-smoke-and-handoff/04-UAT.md` using the evidence set from this plan.
</output>
