---
phase: 12-branch-policy-documentation-and-release-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/release-branch-audit.py
  - scripts/test_release_branch_audit.py
  - tests/fixtures/release-branch-policy.json
autonomous: true
requirements:
  - REL-01
  - SDS-12
must_haves:
  truths:
    - "Every path in the recorded main-to-preview diff receives an aligned, adapted, or excluded classification with policy evidence."
    - "The five main-aligned skill directories are checked for exact source parity, dockerfile-skill is checked against only the documented Railpack delta, and sealos-deploy is manually classified."
    - "The audit preserves preview-owned AGENTS, README, CLAUDE, Kaniko, artifact, and prepare-only behavior while surfacing main-only plugin, distribution, Canvas, and full-deploy surfaces."
  artifacts:
    - path: scripts/release-branch-audit.py
      provides: "File-level main to preview classifier"
    - path: scripts/test_release_branch_audit.py
      provides: "Branch classification and policy regression tests"
    - path: tests/fixtures/release-branch-policy.json
      provides: "Aligned, adapted, excluded, and manually reviewed path policy"
  key_links:
    - from: scripts/release-branch-audit.py
      to: AGENTS.md
      via: "Applies exact aligned-directory, Railpack, Kaniko, and excluded-surface rules"
    - from: scripts/release-branch-audit.py
      to: upstream/brain-deploy-preview
      via: "Reads the immutable target tree without checking it out"
    - from: scripts/release-branch-audit.py
      to: scripts/release-preservation-audit.py
      via: "Includes preservation result and source/target anchors"
---

<objective>
Create the file-level integration boundary audit for main and brain-deploy-preview.

Purpose: Turn branch policy into an explicit release decision record and prevent accidental promotion of full deploy or plugin behavior into the prepare-only branch.
Output: Policy fixture, read-only classifier, and regression tests.
</objective>

<read_first>
  <file>.planning/phases/12-branch-policy-documentation-and-release-audit/12-CONTEXT.md</file>
  <file>.planning/phases/12-branch-policy-documentation-and-release-audit/12-01-PLAN.md</file>
  <file>AGENTS.md</file>
  <file>README.md</file>
  <file>CLAUDE.md</file>
  <file>skills/sealos-deploy/modules/pipeline.md</file>
  <file>skills/k8s-kaniko-job/SKILL.md</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Encode branch classification policy</name>
  <files>tests/fixtures/release-branch-policy.json</files>
  <action>Record the five exact aligned directories, documented dockerfile-skill allowed differences, sealos-deploy manual-review rule, preview-owned files, main-only excluded surfaces, and required prepare/runtime markers. Include a policy version and stable classification IDs.</action>
  <verify><automated>python3 -m json.tool tests/fixtures/release-branch-policy.json >/dev/null</automated></verify>
  <done>Every branch-policy decision has a stable fixture entry.</done>
  <acceptance_criteria>The fixture separates exact parity, allowed adaptation, manual review, and exclusion instead of using a repository-wide ours/theirs rule.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Implement file-level branch audit</name>
  <files>scripts/release-branch-audit.py</files>
  <action>Read git diff/name-status and branch trees, apply the policy fixture, verify aligned-directory parity and dockerfile allowed deltas, require manual sealos-deploy findings, and report every changed path with source path, target path, classification, policy ID, and diagnostic. Reuse the preservation audit anchors and emit machine-readable JSON plus an optional Markdown table.</action>
  <verify><automated>python3 scripts/release-branch-audit.py --root . --fixture tests/fixtures/release-branch-policy.json --source main --target upstream/brain-deploy-preview --candidate HEAD --check</automated></verify>
  <done>The complete main-to-preview boundary is classified with actionable evidence and no branch mutation.</done>
  <acceptance_criteria>Required aligned parity failures, forbidden preview additions, and undocumented dockerfile/deploy differences fail closed; documented adaptations remain visible.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Test classification and target preservation</name>
  <files>scripts/test_release_branch_audit.py</files>
  <action>Test representative aligned, adapted, excluded, and manual-review paths using temporary policy copies and the live branch refs. Assert preview Canvas/plugin/full-deploy exclusions and Kaniko/prepare markers remain explicit.</action>
  <verify><automated>python3 -m unittest scripts.test_release_branch_audit</automated></verify>
  <done>Classification regressions and policy drift are caught before release evidence is published.</done>
  <acceptance_criteria>Tests validate stable output fields and leave branch refs and working-tree files unchanged.</acceptance_criteria>
</task>
</tasks>

<verification>
Run the branch audit, its unit tests, preservation audit, JSON parsing, and `git diff --check`.
</verification>

<success_criteria>
The release candidate has a complete aligned/adapted/excluded file report that proves the preview branch identity and prepare-only boundary.
</success_criteria>
