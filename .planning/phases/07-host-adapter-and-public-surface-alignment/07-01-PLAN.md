---
phase: 07-host-adapter-and-public-surface-alignment
plan: 01
type: execute
wave: 1
depends_on:
  - 06-03
files_modified:
  - commands/sealos.md
  - scripts/skill_design_inventory.py
  - scripts/test_skill_design_inventory.py
  - scripts/test_skill_design_router.py
autonomous: true
requirements:
  - SDS-02
  - SDS-D02

must_haves:
  truths:
    - "The broad route has one parseable record for each physical skill with a readable interaction class and a typed base-plus-escalations risk tuple."
    - "Clear requests select one owner, compound deploy requests expose an ordered handoff with target, inputArtifact, allowedAction, failureReturn, and responseOwner fields, and ambiguous mutation requests stop before side effects."
    - "The route reader remains set-equal to the physical inventory, maps every owner to one documented interaction class, and rejects unknown classes, malformed risk labels, duplicate owners, and missing route records."
  artifacts:
    - path: "commands/sealos.md"
      provides: "Eight-row risk-aware route table and behavior-free ambiguity/handoff policy"
    - path: "scripts/skill_design_inventory.py"
      provides: "Route schema reader, interaction/risk/handoff validation, and typed route diagnostics"
    - path: "scripts/test_skill_design_router.py"
      provides: "Offline route, compound handoff, ambiguity, and mutation fixtures"
  key_links:
    - from: "commands/sealos.md"
      to: "scripts/skill_design_inventory.py"
      via: "One structured route table parsed into RouteEntry records"
    - from: "scripts/skill_design_inventory.py"
      to: "skills/*/SKILL.md"
      via: "Derived inventory set equality and owner lookup"
    - from: "commands/sealos.md"
      to: "scripts/skill_design_inventory.py"
      via: "Five-field handoff steps validate target, inputArtifact, allowedAction, failureReturn, and responseOwner"

---

<objective>
Extend the single broad route into a risk-aware, behavior-free adapter contract that classifies request risk before delegation and records existing compound handoffs.

Purpose: Deliver SDS-02 and SDS-D02 without moving workflow behavior out of the owning skills.
Output: A parseable eight-row route table, typed route reader, and offline route discrimination suite.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
  <file>.planning/phases/07-host-adapter-and-public-surface-alignment/07-CONTEXT.md</file>
  <file>.planning/ROADMAP.md</file>
  <file>.planning/REQUIREMENTS.md</file>
  <file>commands/sealos.md</file>
  <file>scripts/skill_design_inventory.py</file>
  <file>scripts/test_skill_design_inventory.py</file>
  <file>docs/skill-design-system.md</file>
  <file>tests/fixtures/skill-design-baseline.json</file>
  <file>AGENTS.md</file>
</read_first>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-host-adapter-and-public-surface-alignment/07-CONTEXT.md
@.planning/phases/06-inventory-router-and-validator-foundation/06-03-SUMMARY.md
@docs/skill-design-system.md
@commands/sealos.md
@scripts/skill_design_inventory.py
@AGENTS.md
</context>

<acceptance_criteria>
  <criterion>`python3 scripts/skill_design_inventory.py --root . --router commands/sealos.md --check` reports eight route records with validated interaction classes and risk tuples.</criterion>
  <criterion>Route mutation tests prove missing/unknown/duplicate owners, unknown interaction classes and owner mappings, malformed tuple fields, ambiguous mutation stops before provider/filesystem/Kubernetes side effects, and ordered compound handoff metadata with the five required fields and exact diagnostics.</criterion>
  <criterion>Existing inventory tests and all Phase 6 safety/aggregate tests remain green after the route schema extension.</criterion>
</acceptance_criteria>

<artifacts_this_phase_produces>
- `commands/sealos.md`: human-readable eight-owner route table with interaction, risk, and handoff metadata.
- `scripts/skill_design_inventory.py`: importable typed route parser and validator.
- `scripts/test_skill_design_router.py`: standard-library positive and violating route fixtures.
</artifacts_this_phase_produces>

<tasks>
<task type="auto">
  <name>Task 1: Extend the canonical route contract</name>
  <files>commands/sealos.md, scripts/skill_design_inventory.py</files>
  <action>Extend the existing route table with stable columns for interaction class, a typed `base` plus ordered `escalations` capability tuple, and ordered handoff steps. Use the four interaction classes from `docs/skill-design-system.md`, maintain an explicit owner-to-class mapping for all eight skills, validate the five SDS-D02 risk categories and strict escalation order, and encode each handoff step as `target`, `inputArtifact`, `allowedAction`, `failureReturn`, and `responseOwner` fields. Require known owners and repository-local artifact references, preserve host-native plugin and direct-entry syntax, and keep set parity anchored to immediate physical entries. Add an entry-visible policy that ambiguous mutation requests return `stopped` clarification before provider, filesystem, or Kubernetes side effects while detailed confirmation remains in the owning skill.</action>
  <verify>
    <automated>python3 scripts/skill_design_inventory.py --root . --router commands/sealos.md --check</automated>
  </verify>
  <done>The live route parses eight owners, carries typed risk metadata and ordered handoff semantics, and remains readable to maintainers.</done>
</task>
<task type="auto">
  <name>Task 2: Add route discrimination fixtures</name>
  <files>scripts/test_skill_design_inventory.py, scripts/test_skill_design_router.py</files>
  <action>Add temporary-copy tests for clear single-owner selection, every documented owner-to-interaction-class mapping, deploy's complete readiness -> Dockerfile -> Docker-to-Sealos -> deploy/runtime-truth -> Canvas sequence, readiness-to-Dockerfile conditional handoff, and the five required handoff fields. Add violating fixtures for ambiguity that return `stopped` clarification with no provider/filesystem/Kubernetes action marker, missing/unknown/duplicate route owners, unknown interaction classes or owner mappings, malformed risk tuples, unknown handoff owners/fields, CRLF normalization, and route/inventory set mismatch. Assert exact diagnostic code, source path, owner, field, target, and sequence; keep tests offline and preserve the Phase 6 test suite as a prerequisite.</action>
  <verify>
    <automated>python3 scripts/test_skill_design_inventory.py && python3 scripts/test_skill_design_router.py</automated>
  </verify>
  <done>Positive and violating route fixtures distinguish owner selection, risk classification, handoff ordering, and ambiguity handling.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Elevation of Privilege | Ambiguous mutation routing | critical | mitigate | Require an explicit stopped clarification state before any provider, filesystem, or Kubernetes mutation. |
| T-07-02 | Tampering | Route/inventory parity | high | mitigate | Compare parsed route owners against the derived physical set and reject unknown, missing, or duplicate records. |
| T-07-03 | Tampering | Risk escalation metadata | high | mitigate | Validate a typed base-plus-escalations tuple against the fixed five risk categories and owner-specific handoffs. |
</threat_model>

<verification>
Run the route CLI and both inventory/router suites, then inspect that the live table represents the five risk categories and composite handoffs without embedding workflow behavior.
</verification>

<success_criteria>
SDS-02 and SDS-D02 have a single deterministic route source that classifies risk before delegation and proves clear, compound, and ambiguous request outcomes offline.
</success_criteria>

<output>
Create `.planning/phases/07-host-adapter-and-public-surface-alignment/07-01-SUMMARY.md` after execution.
</output>
