---
phase: 02-readme-and-metadata-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - distribution/platforms.json
autonomous: true
requirements:
  - META-01
  - META-02
  - META-04
must_haves:
  truths:
    - "META-04/D-12: `distribution/platforms.json` Codex entry records the native command pair as the primary install path and the `npx plugins` command as alternate compatibility install."
    - "META-04/D-13: Codex evidence references Phase 1 native marketplace add/list/install and installed payload assertions."
    - "META-01/D-14: Registry identity stays aligned on repo `labring/sealos-skills`, marketplace `sealos`, plugin `sealos`, install identity `sealos@sealos`, and label `Sealos`."
    - "META-02/D-16: `.codex-plugin/plugin.json` and root `plugin.json` remain aligned with README identity, asset paths, and `skills: \"./skills/\"`."
    - "D-15: `.agents/plugins/marketplace.json` remains at `source.path: \"./plugins/sealos\"`."
    - "D-17/D-18/D-19: Phase 2 edits stay limited to README/platform metadata and leave validator hardening to Phase 3."
  artifacts:
    - path: "distribution/platforms.json"
      provides: "Codex native install, alternate install, invocation, evidence, and verification date registry"
      contains: "codex plugin marketplace add labring/sealos-skills"
  key_links:
    - from: "distribution/platforms.json Codex install"
      to: ".planning/phases/01-native-marketplace-discovery-contract/evidence/08-phase-1-handoff.md"
      via: "native command pair and evidence wording"
      pattern: "codex plugin marketplace add labring/sealos-skills"
    - from: "distribution/platforms.json Codex evidence"
      to: "scripts/validate-codex-plugin.py"
      via: "existing evidence token"
      pattern: "codex_manifest\\+repo_marketplace"
---

<objective>
Align the Codex platform registry with the README and Phase 1 native marketplace evidence.

Purpose: Make metadata support the same Codex install, invocation, identity, and evidence contract that README teaches.
Output: Updated `distribution/platforms.json` Codex entry plus validation that Codex manifests and runtime skills remain aligned.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-readme-and-metadata-alignment/02-CONTEXT.md
@.planning/phases/02-readme-and-metadata-alignment/02-RESEARCH.md
@.planning/phases/02-readme-and-metadata-alignment/02-PATTERNS.md
@.planning/phases/02-readme-and-metadata-alignment/02-01-SUMMARY.md
@.planning/phases/01-native-marketplace-discovery-contract/01-VERIFICATION.md
@.planning/phases/01-native-marketplace-discovery-contract/evidence/08-phase-1-handoff.md
@README.md
@distribution/platforms.json
@.codex-plugin/plugin.json
@plugin.json
@.agents/plugins/marketplace.json
@scripts/validate-codex-plugin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the Codex platform registry entry</name>
  <files>distribution/platforms.json</files>
  <action>Update only the Codex platform entry and top-level registry freshness fields. Set top-level `lastUpdated` to `2026-06-15`. Set Codex `install` to `codex plugin marketplace add labring/sealos-skills && codex plugin add sealos@sealos` per D-12. Add or update Codex `alternateInstall` with `npx plugins add https://github.com/labring/sealos-skills --target codex` per D-03 and D-12. Keep `invoke` Codex-specific with `$sealos` for Codex CLI and selecting `Sealos` from Plugins in Codex App per D-08 and D-14. Update `evidence` to cite Phase 1 native marketplace add/list/install, installed payload assertions, and include the existing validator token `codex_manifest+repo_marketplace` per D-13. Set Codex `lastVerified` to `2026-06-15`. Preserve other platform entries per D-18.</action>
  <verify>
    <automated>python3 -m json.tool distribution/platforms.json >/dev/null && python3 -c 'import json; p=json.load(open("distribution/platforms.json")); c=[x for x in p["platforms"] if x["id"]=="codex"][0]; assert p["lastUpdated"]=="2026-06-15"; assert c["install"]=="codex plugin marketplace add labring/sealos-skills && codex plugin add sealos@sealos"; assert c["alternateInstall"]=="npx plugins add https://github.com/labring/sealos-skills --target codex"; assert "sealos@sealos" in c["install"]; assert "codex_manifest+repo_marketplace" in c["evidence"]; assert "Phase 1" in c["evidence"]; assert c["lastVerified"]=="2026-06-15"' && rg -n 'codex plugin marketplace add labring/sealos-skills|codex plugin add sealos@sealos|npx plugins add https://github.com/labring/sealos-skills --target codex|codex_manifest\\+repo_marketplace' distribution/platforms.json</automated>
  </verify>
  <done>`distribution/platforms.json` satisfies META-01 and META-04 with native install, alternate install, invocation, evidence, and verification date.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 2 metadata and scope validation</name>
  <files>distribution/platforms.json</files>
  <action>Run the existing validation suite after README and registry alignment per D-19. Run `python3 scripts/validate-codex-plugin.py`, JSON syntax checks for `distribution/platforms.json`, and JSON checks for `.codex-plugin/plugin.json`, `plugin.json`, and `.agents/plugins/marketplace.json` if execution touched any of those manifests. Verify README and registry both contain the native command pair and the compatibility/local command. Verify `.agents/plugins/marketplace.json` still contains `source.path: "./plugins/sealos"` per D-15, and verify `.codex-plugin/plugin.json` plus root `plugin.json` still contain `skills: "./skills/"` per D-16. Run `git diff -- skills --exit-code` per D-17.</action>
  <verify>
    <automated>python3 scripts/validate-codex-plugin.py && python3 -m json.tool distribution/platforms.json >/dev/null && python3 -m json.tool .codex-plugin/plugin.json >/dev/null && python3 -m json.tool plugin.json >/dev/null && python3 -m json.tool .agents/plugins/marketplace.json >/dev/null && rg -n 'codex plugin marketplace add labring/sealos-skills|codex plugin add sealos@sealos|npx plugins add https://github.com/labring/sealos-skills --target codex' README.md distribution/platforms.json && rg -n '\"path\": \"\\./plugins/sealos\"' .agents/plugins/marketplace.json && rg -n '\"skills\": \"\\./skills/\"' .codex-plugin/plugin.json plugin.json && git diff -- skills --exit-code</automated>
  </verify>
  <done>Existing validator passes, JSON syntax passes for Codex-facing manifests, README and registry share the same command contract, Codex marketplace source remains `./plugins/sealos`, manifests keep root `./skills/`, and runtime `skills/**` files remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry to external host support claim | `distribution/platforms.json` describes what users and maintainers can rely on for Codex support. |
| Registry to validator | Existing validator consumes the Codex evidence field and command support claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | Codex platform install identity | mitigate | Assert repository, marketplace, plugin, install identity, and display label through exact JSON checks and `rg`. |
| T-02-06 | Tampering | Codex evidence string | mitigate | Preserve `codex_manifest+repo_marketplace` and add Phase 1 evidence wording; run existing validator. |
| T-02-07 | Repudiation | Verification date | mitigate | Set `lastVerified` and top-level `lastUpdated` to `2026-06-15` for the Phase 1 evidence-backed registry update. |
| T-02-08 | Tampering | runtime skill source | mitigate | Run `git diff -- skills --exit-code`. |
</threat_model>

<verification>
- `python3 scripts/validate-codex-plugin.py`
- `python3 -m json.tool distribution/platforms.json >/dev/null`
- `python3 -m json.tool .codex-plugin/plugin.json >/dev/null`
- `python3 -m json.tool plugin.json >/dev/null`
- `python3 -m json.tool .agents/plugins/marketplace.json >/dev/null`
- `rg -n 'codex plugin marketplace add labring/sealos-skills' README.md distribution/platforms.json`
- `rg -n 'codex plugin add sealos@sealos' README.md distribution/platforms.json`
- `rg -n 'npx plugins add https://github.com/labring/sealos-skills --target codex' README.md distribution/platforms.json`
- `rg -n '"path": "\\./plugins/sealos"' .agents/plugins/marketplace.json`
- `rg -n '"skills": "\\./skills/"' .codex-plugin/plugin.json plugin.json`
- `git diff -- skills --exit-code`
</verification>

<success_criteria>
Codex platform metadata records the verified native install path, compatibility/local path, invocation wording, Phase 1 evidence, and verification date while existing Codex manifests keep identity and root skill-source alignment.
</success_criteria>

<output>
Create `.planning/phases/02-readme-and-metadata-alignment/02-02-SUMMARY.md` when done.
</output>
