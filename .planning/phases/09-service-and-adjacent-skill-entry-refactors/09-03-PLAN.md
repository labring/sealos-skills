---
phase: 09-service-and-adjacent-skill-entry-refactors
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - skills/sealos-canvas/SKILL.md
  - skills/sealos-canvas/scripts/generate-canvas.mjs
  - skills/sealos-canvas/evals/evals.json
  - scripts/test_canvas_contract.mjs
autonomous: true
requirements:
  - SDS-04
  - SDS-D06
must_haves:
  truths:
    - "Canvas stops before generation when deployed state, kubeconfig, kubectl, or read access is unavailable."
    - "Canvas uses only read commands, sanitizes Secret/ConfigMap/error data, and never owns deployment mutation."
    - "Canvas returns local URL/cache and graph evidence with an explicit server shutdown condition."
  artifacts:
    - path: skills/sealos-canvas/SKILL.md
      provides: "Read-only precondition, terminal output, sanitization, and lifecycle contract"
    - path: skills/sealos-canvas/scripts/generate-canvas.mjs
      provides: "Fixture-capable read-only generator and redacted diagnostics"
    - path: scripts/test_canvas_contract.mjs
      provides: "Offline stop, fixture, sanitization, and mutation-scan tests"
  key_links:
    - from: skills/sealos-canvas/SKILL.md
      to: skills/sealos-canvas/scripts/generate-canvas.mjs
      via: "Conditional generator loading after deployed/read-only canaries"
    - from: scripts/test_canvas_contract.mjs
      to: skills/sealos-canvas/evals/evals.json
      via: "Fixture scenarios for stop, local URL, topology, and sanitization"
---

<objective>
Refocus Canvas around verified deployed state, read-only access, sanitized JSON, exact outputs, and local server lifecycle.

Purpose: Make the observation boundary independently testable before deploy orchestration consumes its handoff.
Output: Hardened generator diagnostics, explicit output/lifecycle contract, and deterministic offline tests.
</objective>

<read_first>
  <file>.planning/phases/09-service-and-adjacent-skill-entry-refactors/09-CONTEXT.md</file>
  <file>skills/sealos-canvas/SKILL.md</file>
  <file>skills/sealos-canvas/scripts/generate-canvas.mjs</file>
  <file>skills/sealos-canvas/assets/canvas-template.html</file>
  <file>skills/sealos-canvas/evals/evals.json</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Harden Canvas entry and generator contracts</name>
  <files>skills/sealos-canvas/SKILL.md, skills/sealos-canvas/scripts/generate-canvas.mjs</files>
  <action>Keep deployed-state, kubeconfig, kubectl, and read-only preconditions first. Define exact success/stopped/error payload fields including local_url/html_path/app_url/node_count/edge_count, sanitized evidence, and server_lifetime. Keep fixture mode and --no-serve. Sanitize kubectl/read/server diagnostics before they reach JSON or HTML, preserve Secret/ConfigMap summaries, and document SIGINT/SIGTERM/request-end shutdown without adding any mutation command.</action>
  <verify><automated>node --check skills/sealos-canvas/scripts/generate-canvas.mjs</automated></verify>
  <done>The generator has a bounded read-only data surface and no raw credential-shaped diagnostics.</done>
</task>
<task type="auto">
  <name>Task 2: Add deterministic Canvas contract tests and eval assertions</name>
  <files>scripts/test_canvas_contract.mjs, skills/sealos-canvas/evals/evals.json</files>
  <action>Add tests using temporary workdirs and SEALOS_CANVAS_KUBE_FIXTURE: missing state stops without HTML, a fixture generates sanitized HTML with counts and edges, secret/config values stay absent, and source text contains no kubectl mutation verbs. Keep eval prompts/assertions aligned with the exact JSON fields and temporary server lifecycle.</action>
  <verify><automated>node scripts/test_canvas_contract.mjs</automated></verify>
  <done>Canvas stop, positive fixture, sanitization, and mutation-boundary cases are deterministic and provider-free.</done>
</task>
</tasks>

<verification>
Run Node syntax, Canvas contract tests, existing baseline/safety/aggregate checks, and `git diff --check`. Do not start a live Canvas server in the repository during validation.
</verification>

<success_criteria>
Canvas proves the read-only, sanitized, deployed-state, local-URL, and bounded-lifecycle requirements offline.
</success_criteria>
