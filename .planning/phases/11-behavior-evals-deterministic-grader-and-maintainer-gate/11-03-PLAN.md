---
phase: 11-behavior-evals-deterministic-grader-and-maintainer-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - scripts/maintainer-quality-gate.py
  - scripts/test_maintainer_quality_gate.py
  - tests/fixtures/maintainer-quality-gate.json
autonomous: true
requirements:
  - SDS-07
  - SDS-10
  - SDS-11
must_haves:
  truths:
    - "One offline command runs contract shape, inventory, routes, versions, links, safety canaries, eval schemas, behavior probes, and existing skill-specific validators."
    - "The aggregate command returns a nonzero exit code for any failed component and preserves actionable structured diagnostics."
    - "Missing optional provider or system prerequisites are reported as conditional checks with safe guidance while the required offline gate remains deterministic."
  artifacts:
    - path: scripts/maintainer-quality-gate.py
      provides: "Single standard-library maintainer gate"
    - path: scripts/test_maintainer_quality_gate.py
      provides: "Gate composition, failure, and redaction tests"
    - path: tests/fixtures/maintainer-quality-gate.json
      provides: "Command registry and expected component contract"
  key_links:
    - from: scripts/maintainer-quality-gate.py
      to: scripts/validate_skill_design.py
      via: "Composed structural and safety validation step"
    - from: scripts/maintainer-quality-gate.py
      to: scripts/skill-design-behavior.mjs
      via: "Composed structured-trace behavior step"
    - from: scripts/maintainer-quality-gate.py
      to: scripts/test_dependency_skill_gates.py
      via: "Existing dependency and quality-gate preservation step"
---

<objective>
Create one documented, machine-readable maintainer quality gate.

Purpose: Make the complete design-system contract runnable from a clean checkout with actionable failures.
Output: Standard-library gate runner, component registry fixture, and regression tests for pass/fail composition.
</objective>

<read_first>
  <file>.planning/phases/11-behavior-evals-deterministic-grader-and-maintainer-gate/11-CONTEXT.md</file>
  <file>.planning/phases/11-behavior-evals-deterministic-grader-and-maintainer-gate/11-02-PLAN.md</file>
  <file>scripts/validate_skill_design.py</file>
  <file>scripts/test_dependency_skill_gates.py</file>
  <file>scripts/skill-design-behavior.mjs</file>
  <file>scripts/test-skill-design-baseline.mjs</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Define the gate component registry</name>
  <files>tests/fixtures/maintainer-quality-gate.json</files>
  <action>Record the ordered offline components, command arguments, ownership, expected result semantics, optional-prerequisite policy, and stable component IDs. Include structural validators, inventory/router, versions/links, safety, eval schema, behavior, baseline, and existing skill-specific suites.</action>
  <verify><automated>python3 -m json.tool tests/fixtures/maintainer-quality-gate.json >/dev/null</automated></verify>
  <done>The complete gate sequence is explicit and machine-readable.</done>
  <acceptance_criteria>Each component has a deterministic command, owner, and actionable failure category.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Implement the aggregate gate</name>
  <files>scripts/maintainer-quality-gate.py</files>
  <action>Run the registry commands in order with repository-root working directory, capture stdout/stderr and durations, classify pass/fail/conditional, redact credential-shaped output, and emit JSON containing component summaries plus an aggregate exit status. Keep provider mutations and installation commands outside the registry.</action>
  <verify><automated>python3 scripts/maintainer-quality-gate.py --root . --fixture tests/fixtures/maintainer-quality-gate.json --check</automated></verify>
  <done>The gate runs the complete offline sequence and produces stable JSON diagnostics.</done>
  <acceptance_criteria>A failing component makes the command exit nonzero and names its command, owner, and captured safe diagnostic.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Test gate failure and redaction behavior</name>
  <files>scripts/test_maintainer_quality_gate.py</files>
  <action>Use temporary fixture copies and a harmless failing command to prove aggregate failure semantics, inspect JSON component IDs and statuses, verify optional prerequisite classification, and assert credential-shaped subprocess output is redacted in the report.</action>
  <verify><automated>python3 -m unittest scripts.test_maintainer_quality_gate</automated></verify>
  <done>Gate regression tests cover composition, failure propagation, conditional status, and output redaction.</done>
  <acceptance_criteria>Tests exercise the runner behavior through its public CLI and preserve provider-free execution.</acceptance_criteria>
</task>
</tasks>

<verification>
Run the aggregate gate, its unit tests, all JSON parsing, and `git diff --check`.
</verification>

<success_criteria>
Maintainers have one offline command with stable JSON output and fail-closed aggregate semantics.
</success_criteria>
