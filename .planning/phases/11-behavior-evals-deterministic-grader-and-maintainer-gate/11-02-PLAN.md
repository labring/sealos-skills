---
phase: 11-behavior-evals-deterministic-grader-and-maintainer-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scripts/skill-design-baseline.mjs
  - scripts/test-skill-design-baseline.mjs
  - scripts/skill-design-behavior.mjs
  - scripts/test_skill_design_behavior.mjs
  - tests/fixtures/skill-design-behavior.json
autonomous: true
requirements:
  - SDS-07
  - SDS-10
must_haves:
  truths:
    - "The deterministic grader evaluates `{text, toolCalls, files}` plus evidence, safeNextAction, and coverage traces and distinguishes positive success from violating stopped/error outcomes."
    - "The grader reports actionable skill, case, field, guard, and source diagnostics in stable machine-readable output."
    - "Temporary-copy mutation tests exercise the real validators and catch removed safety, output, progressive-loading, and handoff evidence."
  artifacts:
    - path: scripts/skill-design-behavior.mjs
      provides: "Structured-trace behavior grader"
    - path: scripts/test_skill_design_behavior.mjs
      provides: "Positive/violating grader and mutation regression tests"
    - path: tests/fixtures/skill-design-behavior.json
      provides: "Deterministic trace and mutation scenarios"
  key_links:
    - from: scripts/skill-design-behavior.mjs
      to: scripts/skill-design-baseline.mjs
      via: "Shared baseline parsing and canonical skill ownership"
    - from: tests/fixtures/skill-design-behavior.json
      to: scripts/test_skill_design-behavior.mjs
      via: "Fixture-driven red/green assertions"
---

<objective>
Implement the structured-trace behavior grader and its deterministic regression tests.

Purpose: Turn the Phase 5 baseline and Phase 11 eval inputs into a repeatable behavior oracle.
Output: Shared text evidence validation, machine-readable grader results, and real temporary-copy mutation checks.
</objective>

<read_first>
  <file>.planning/phases/11-behavior-evals-deterministic-grader-and-maintainer-gate/11-CONTEXT.md</file>
  <file>.planning/phases/11-behavior-evals-deterministic-grader-and-maintainer-gate/11-01-PLAN.md</file>
  <file>scripts/skill-design-baseline.mjs</file>
  <file>scripts/test-skill-design-baseline.mjs</file>
  <file>tests/fixtures/skill-design-baseline.json</file>
  <file>scripts/skill_design_safety.py</file>
  <file>scripts/test_skill_design_safety.py</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Enforce observable trace fields</name>
  <files>scripts/skill-design-baseline.mjs, scripts/test-skill-design-baseline.mjs</files>
  <action>Extend the canonical required field set and validation to require a non-empty redacted `text` string, named `evidence`, a non-empty `safeNextAction`, and coverage entries for routing, boundary, terminal, progressive-loading, and highest-risk. Retain existing tool-call, file, terminal, handoff, and redaction checks and add regression assertions for missing fields.</action>
  <verify><automated>node --check scripts/skill-design-baseline.mjs && node scripts/skill-design-baseline.mjs --fixture tests/fixtures/skill-design-baseline.json --check</automated></verify>
  <done>Baseline traces enforce the complete observable evidence tuple.</done>
  <acceptance_criteria>A missing or secret-shaped text/evidence/safe-next-action/coverage value produces a targeted nonzero validation result.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Implement the behavior grader</name>
  <files>scripts/skill-design-behavior.mjs, tests/fixtures/skill-design-behavior.json</files>
  <action>Build a provider-free ESM grader that consumes the baseline fixture and optional behavior scenarios, checks each case's owner, interaction class, terminal state, loaded resources, evidence tuple, evidence list, safe next action, five coverage dimensions, handoff, guard, and redaction, and emits stable JSON with `ok`, per-case diagnostics, and aggregate counts. Reject unsafe paths and credential-shaped values before grading outcome.</action>
  <verify><automated>node --check scripts/skill-design-behavior.mjs && node scripts/skill-design-behavior.mjs --fixture tests/fixtures/skill-design-baseline.json --scenarios tests/fixtures/skill-design-behavior.json --check</automated></verify>
  <done>The grader emits actionable pass/fail reports for all positive and violating traces.</done>
  <acceptance_criteria>All committed positive traces pass; every violating trace has a stopped/error state and a named guard.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add real mutation regressions</name>
  <files>scripts/test_skill_design_behavior.mjs</files>
  <action>Copy the repository into temporary directories, remove one live canary or required evidence marker at a time, invoke the actual validator/grader, and assert the resulting diagnostic identifies the mutated skill, case, and guard. Cover confirmation, redaction, read-only, eligibility, runtime acceptance, quality-gate, output, and handoff mutations.</action>
  <verify><automated>node --test scripts/test_skill_design_behavior.mjs</automated></verify>
  <done>Mutation tests fail against the real copied sources and pass against the live repository.</done>
  <acceptance_criteria>Tests prove semantic guard removal is detected by the owning validator rather than by a string self-check.</acceptance_criteria>
</task>
</tasks>

<verification>
Run the baseline checker, behavior grader, `node --test scripts/test_skill_design_behavior.mjs`, and `git diff --check`.
</verification>

<success_criteria>
A deterministic structured-trace grader and real mutation gate cover the Phase 11 behavior contract.
</success_criteria>
