---
phase: 05-baseline-ownership-and-shared-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/skill-design-baseline.md
  - tests/fixtures/skill-design-baseline.json
  - scripts/skill-design-baseline.mjs
  - scripts/test-skill-design-baseline.mjs
autonomous: true
requirements:
  - SDS-01
  - SDS-03
must_haves:
  truths:
    - "The baseline names exactly the eight physical SKILL.md entries and records their current ownership, adapter projection, lifecycle, risk, output, handoff, and runtime-artifact behavior."
    - "Every canonical skill has one deterministic positive trace and one deterministic violating trace covering the current routing, boundary, terminal, and applicable handoff behavior."
    - "Baseline evidence is offline, structured, redaction-aware, and reusable by later validator and behavior-eval phases."
  artifacts:
    - path: "docs/skill-design-baseline.md"
      provides: "Human-readable eight-skill ownership, adapter, runtime artifact, and preservation matrix"
    - path: "tests/fixtures/skill-design-baseline.json"
      provides: "Machine-readable positive and violating baseline cases with observable trace fields"
    - path: "scripts/skill-design-baseline.mjs"
      provides: "Dependency-free fixture checker and structured baseline report command"
    - path: "scripts/test-skill-design-baseline.mjs"
      provides: "Node built-in tests proving baseline fixture discrimination"
  key_links:
    - from: "tests/fixtures/skill-design-baseline.json"
      to: "scripts/skill-design-baseline.mjs"
      via: "The checker parses the fixture schema, resolves source paths, and validates trace fields"
    - from: "docs/skill-design-baseline.md"
      to: "skills/*/SKILL.md and commands/sealos.md"
      via: "Matrix rows cite the physical entry and current broad-router ownership without changing projections"
    - from: "tests/fixtures/skill-design-baseline.json"
      to: "scripts/test-skill-design-baseline.mjs"
      via: "Each skill's positive case must pass and violating case must report its named guard"
---

<objective>
Capture the current eight-skill behavior and ownership baseline before any entry refactor.

Purpose: Establish D-03 source ownership and D-10/D-11/D-12 observable evidence so later contract edits can prove preservation rather than infer it from Markdown shape.
Output: A current-state matrix, a 16-case deterministic trace fixture, and an offline checker/test pair that later phases can reuse.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-CONTEXT.md
@AGENTS.md
@commands/sealos.md
@.codex-plugin/plugin.json
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@.codebuddy-plugin/marketplace.json
@.qoder-plugin/plugin.json
@distribution/platforms.json
@gemini-extension.json
@qwen-extension.json
@openclaw.plugin.json
@skills/cloud-native-readiness/SKILL.md
@skills/dockerfile-skill/SKILL.md
@skills/docker-to-sealos/SKILL.md
@skills/sealos-deploy/SKILL.md
@skills/sealos-database/SKILL.md
@skills/sealos-s3/SKILL.md
@skills/sealos-canvas/SKILL.md
@skills/sealos-app-builder/SKILL.md
@skills/sealos-deploy/evals/evals.json
@skills/sealos-database/evals/evals.json
@skills/sealos-s3/evals/evals.json
@skills/sealos-canvas/evals/evals.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the physical ownership and runtime baseline</name>
  <files>docs/skill-design-baseline.md</files>
  <action>Read the eight `skills/*/SKILL.md` files, `commands/sealos.md`, every current host manifest or context adapter, and the four existing skill eval suites. Write a matrix with exactly one row for each canonical skill: entry path and trigger aliases; owned outcome and interaction class; accepted inputs, preconditions, and mutation boundary; current success, stopped, and error evidence; downstream handoff target, input artifact, allowed action, failure return, and response owner; current host projection names and invocation syntax; and generated target-project artifacts such as `.sealos/analysis.json`, `.sealos/build/build-result.json`, `.sealos/template/index.yaml`, `.sealos/state.json`, and `.sealos/canvas/index.html`. Record the physical `skills/` entries as behavior owners, `commands/sealos.md` as the broad route owner, and host files as projections per D-03. Preserve current drift observations, including seven-versus-eight explicit host lists, as baseline facts for later phases; keep projection repair outside this plan. Include the existing deterministic validator and skill-local gate commands as the runtime preservation oracle from D-11, and cite the trace fields required by D-12.</action>
  <verify>
    <automated>test -f docs/skill-design-baseline.md && python3 -c "from pathlib import Path; t=Path('docs/skill-design-baseline.md').read_text(); names=['cloud-native-readiness','dockerfile-skill','docker-to-sealos','sealos-deploy','sealos-database','sealos-s3','sealos-canvas','sealos-app-builder']; assert all(n in t for n in names); assert all(k in t for k in ['selected owner','loaded resource','tool call','terminal state','redaction','handoff','runtime artifact'])"</automated>
  </verify>
  <done>The baseline document contains one auditable row per physical skill, identifies the two source-of-truth boundaries, records current adapter/runtime facts and the preservation gates, and names all observable trace fields.</done>
</task>

<task type="auto">
  <name>Task 2: Define the deterministic baseline trace fixture and checker</name>
  <files>tests/fixtures/skill-design-baseline.json, scripts/skill-design-baseline.mjs</files>
  <action>Create a `schemaVersion: 1` JSON fixture containing eight skill records, each with one `positive` and one `violating` case. Cover these current behaviors: readiness eligibility success plus fail-closed stop; Dockerfile build-and-runtime acceptance plus build-only success violation; Docker-to-Sealos rule-registry/MUST-map quality-gate success plus missing-rule evidence violation; deploy Runtime Truth success plus missing App URL or cleanup-confirmation violation; database create-or-reuse and redacted connectivity plus unconfirmed public/destructive mutation; S3 private bucket/object proof plus unconfirmed public or credential rotation; Canvas deployed read-only local URL plus missing state/kubeconfig or mutation command; and App Builder SDK/iframe/publish evidence plus code-versus-tutorial or missing Desktop verification. Give every case `caseId`, `prompt`, `sourceRefs`, `expectedOwner`, `interactionClass`, `terminalState`, `loadedResources`, `toolCalls`, `files`, `handoff`, `redactionChecks`, and a named `guard` for violating cases. Implement `scripts/skill-design-baseline.mjs` as an importable function and CLI that parses the fixture with standard-library Node APIs, resolves every cited repository path, checks one-level owned-resource references, validates terminal/evidence fields, scans trace values for credential-shaped data, and prints a structured summary containing the selected owner, terminal state, artifacts, handoff, and redaction result. Keep execution offline and deterministic per D-11; use current eval prompts and entry evidence as fixture sources rather than live provider calls.</action>
  <verify>
    <automated>node scripts/skill-design-baseline.mjs --fixture tests/fixtures/skill-design-baseline.json --check</automated>
  </verify>
  <done>The fixture contains exactly 16 cases across eight skills, positive cases model current success/evidence paths, violating cases model current stop/confirmation/refusal paths, and the CLI rejects malformed paths, missing evidence, deep disclosure, or sensitive trace values with case-specific diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Add red/green tests for baseline discrimination</name>
  <files>scripts/test-skill-design-baseline.mjs</files>
  <action>Use `node:test` and `node:assert/strict` to import the checker and exercise the committed fixture. Assert that all eight positive cases produce `ok: true` with their expected owner and terminal state, all eight violating cases produce `ok: false` with the declared guard and a stopped or error terminal state, every case reports observable resource/tool/artifact fields, and every redaction check passes. Add a fixture-count assertion for exactly eight skills and two cases per skill so a future omission fails with the skill name. Keep the tests provider-free and suitable for a sub-minute local run.</action>
  <verify>
    <automated>node --test scripts/test-skill-design-baseline.mjs</automated>
  </verify>
  <done>The baseline test suite proves positive/violating discrimination, eight-skill coverage, trace completeness, and secret-safe evidence through deterministic assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical entry files -> baseline document | Authored Markdown is converted into evidence claims about ownership and safety. |
| Baseline JSON -> Node checker | Fixture content controls which traces are accepted as current behavior. |
| Trace output -> committed evidence | Tool calls, file paths, and terminal text can accidentally carry credentials or kubeconfig data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `tests/fixtures/skill-design-baseline.json` | high | mitigate | Require exactly eight owners, two cases per owner, source-path resolution, named violating guards, and deterministic red/green tests. |
| T-05-02 | Information Disclosure | Baseline traces and `docs/skill-design-baseline.md` | high | mitigate | Add credential-shaped value checks, redaction assertions, allowlisted evidence fields, and a rule that real auth, env, kubeconfig, and connection values never enter fixtures. |
| T-05-03 | Repudiation | Baseline matrix and existing runtime gates | medium | mitigate | Record source references, command names, artifact paths, and observed terminal evidence so later refactors can compare against a dated, reproducible baseline. |
</threat_model>

<verification>
Run `node scripts/skill-design-baseline.mjs --fixture tests/fixtures/skill-design-baseline.json --check` and `node --test scripts/test-skill-design-baseline.mjs`. Confirm the matrix names all eight entries and the fixture/checker report remains offline, deterministic, and redaction-safe.
</verification>

<success_criteria>
- `docs/skill-design-baseline.md` records exactly eight physical owners, current adapter projections, runtime artifacts, handoffs, and preservation gates.
- The fixture and checker represent exactly 16 baseline cases with structured observable traces.
- The Node test suite passes and distinguishes every positive case from its violating counterpart.
</success_criteria>

<output>
Create `.planning/phases/05-baseline-ownership-and-shared-contract/05-01-SUMMARY.md` when done.
</output>
