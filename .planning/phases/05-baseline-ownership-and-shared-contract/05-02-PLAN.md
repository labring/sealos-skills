---
phase: 05-baseline-ownership-and-shared-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - docs/skill-design-system.md
  - docs/skill-safety-canaries.md
autonomous: true
requirements:
  - SDS-01
  - SDS-03
  - SDS-05
must_haves:
  truths:
    - "One ordered core contract defines the same discovery, scope, risk, workflow, progressive-loading, output, handoff, lifecycle, and verification facets for every entry."
    - "Lifecycle vocabulary is request-scoped and gives every skill explicit success, stopped, and error terminal semantics, with Canvas server lifetime and shutdown evidence."
    - "A per-skill safety-canary matrix names the entry-visible guard, trigger, and required evidence for confirmation, redaction, kubeconfig, read-only, eligibility, quality-gate, and runtime-acceptance risks where they apply."
  artifacts:
    - path: "docs/skill-design-system.md"
      provides: "Shared entry contract template, lifecycle vocabulary, risk classes, progressive-loading rules, and ownership boundaries"
    - path: "docs/skill-safety-canaries.md"
      provides: "Eight-skill safety canary registry with stable IDs, phrases, triggers, and evidence requirements"
  key_links:
    - from: "docs/skill-design-system.md"
      to: "skills/*/SKILL.md"
      via: "The exact core section order is the skeleton applied by Plan 05-03"
    - from: "docs/skill-safety-canaries.md"
      to: "skills/*/SKILL.md"
      via: "Each canary ID resolves to an entry-visible rule and its applicable evidence check"
    - from: "docs/skill-design-baseline.md"
      to: "docs/skill-design-system.md"
      via: "Contract vocabulary reuses baseline ownership, terminal, handoff, and artifact fields"
---

<objective>
Define the shared entry contract, lifecycle vocabulary, risk classes, and entry-visible safety canaries.

Purpose: Turn D-01 through D-09 into a deterministic skeleton that keeps domain evidence owned by each skill while making load-bearing rules visible before detailed instructions load.
Output: A maintainer-facing contract document and a per-skill canary registry consumed by the eight entry refactors.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-CONTEXT.md
@docs/skill-design-baseline.md
@tests/fixtures/skill-design-baseline.json
@commands/sealos.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the shared entry contract and lifecycle vocabulary</name>
  <files>docs/skill-design-system.md</files>
  <action>Write the shared contract with this exact ordered core: `Identity and Discovery`, `Scope and Boundaries`, `Risk and Confirmation`, `Lifecycle Workflow`, `Progressive Disclosure`, `Output, Stop, and Error States`, `Handoffs`, and `Verification`. Define the required fields and observable evidence for each facet, then permit domain-specific extension sections after the core per D-02. Define request-scoped lifecycle language and the terminal vocabulary from D-04 and D-05: `success` carries the strongest domain evidence and artifact or URL paths; `stopped` names the unmet precondition or confirmation boundary plus the safe next action; `error` names the failed step or artifact plus the recovery action with sensitive values redacted. State the Canvas-specific loopback server lifetime and shutdown condition from D-06. Document the interaction classes (read-only observation, local artifact mutation, cloud/local mutation, and composite orchestration), one-level owned reference loading with explicit conditions, typed handoff fields, and the D-03 ownership split where root `skills/` entries own behavior and host/router files project it. Keep universal output JSON and line-count rules outside the contract.</action>
  <verify>
    <automated>test -f docs/skill-design-system.md && python3 -c "from pathlib import Path; t=Path('docs/skill-design-system.md').read_text(); headings=['Identity and Discovery','Scope and Boundaries','Risk and Confirmation','Lifecycle Workflow','Progressive Disclosure','Output, Stop, and Error States','Handoffs','Verification']; assert all(('## '+h) in t for h in headings); assert all(k in t for k in ['success','stopped','error','request-scoped','Canvas','one level','root skills'])"</automated>
  </verify>
  <done>The contract document defines the required ordered facets, request-scoped terminal states, Canvas lifetime, risk classes, one-level loading, handoff fields, domain extension rule, and source ownership in language ready for all eight entries.</done>
</task>

<task type="auto">
  <name>Task 2: Define per-skill safety canaries and evidence requirements</name>
  <files>docs/skill-safety-canaries.md</files>
  <action>Build a matrix with one row per canonical skill and stable canary IDs for every applicable entry-visible rule in D-07. Cover readiness eligibility stops; Dockerfile runtime acceptance, owned-file and secret boundaries; Docker-to-Sealos rule precedence, MUST-map/rules-registry coupling, and complete quality-gate requirement; deploy Sealos kubeconfig scope, system-tool installation confirmation, deletion/public-exposure/credential confirmation, redaction, and Runtime Truth acceptance; database and S3 redaction, env-preservation, create-or-reuse, public/destructive/rotation confirmation, and connectivity or object-flow evidence; Canvas deployed-state precondition, read-only command boundary, sanitized Secret/ConfigMap output, kubeconfig scope, and temporary-server shutdown; and App Builder code/tutorial boundary, SDK-source precedence, real Desktop iframe verification, and publish handoff. For each canary record the owning `SKILL.md`, a specific phrase or normalized marker, its trigger condition, required observable evidence, and the baseline case IDs from Plan 05-01. State that the entry remains the policy source and the matrix supplies stable IDs for later mutation checks. Preserve D-08's load-bearing MUST-map and registry coupling and D-09's explicit confirmation semantics.</action>
  <verify>
    <automated>test -f docs/skill-safety-canaries.md && python3 -c "from pathlib import Path; t=Path('docs/skill-safety-canaries.md').read_text(); names=['cloud-native-readiness','dockerfile-skill','docker-to-sealos','sealos-deploy','sealos-database','sealos-s3','sealos-canvas','sealos-app-builder']; assert all(n in t for n in names); assert all(k in t for k in ['canary','trigger','evidence','confirmation','redaction','kubeconfig','read-only','eligibility','quality gate','runtime'])"</automated>
  </verify>
  <done>The canary registry has complete eight-skill coverage, stable IDs and mutation targets, explicit triggers/evidence, and preserved Sealos-specific safety semantics for every applicable risk class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract document -> entry skeleton | Maintainer-authored vocabulary controls what an agent sees before loading branch detail. |
| Canary matrix -> future static/behavior checks | Canary IDs and markers define the expected safety surface for later validators. |
| Domain rules -> shared vocabulary | Compression can erase Sealos-specific confirmation, redaction, or quality-gate requirements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | `docs/skill-design-system.md` | high | mitigate | Require the exact eight-section order, explicit lifecycle fields, and source-ownership statements before Plan 05-03 consumes the document. |
| T-05-05 | Elevation of Privilege | `docs/skill-safety-canaries.md` | critical | mitigate | Give every applicable mutation a named entry canary, trigger, confirmation condition, and evidence target; preserve the owning Markdown policy as the final source. |
| T-05-06 | Information Disclosure | Safety and output vocabulary | high | mitigate | Require redaction markers and secret-safe evidence for credentials, kubeconfig, env values, and complete connection strings in every relevant row. |
</threat_model>

<verification>
Run the heading and coverage assertions for both documents. Cross-check each canary row against `docs/skill-design-baseline.md` and `tests/fixtures/skill-design-baseline.json`, then confirm the matrix preserves D-08 MUST-map/registry coupling and D-09 confirmation semantics.
</verification>

<success_criteria>
- The shared contract document exposes the ordered core, terminal vocabulary, Canvas lifecycle, risk classes, one-level loading, handoff shape, and ownership rules.
- The safety-canary document maps every applicable canary to all eight entries with trigger and evidence fields.
- All D-01 through D-09 decisions have a direct, traceable contract statement for Plan 05-03.
</success_criteria>

<output>
Create `.planning/phases/05-baseline-ownership-and-shared-contract/05-02-SUMMARY.md` when done.
</output>
