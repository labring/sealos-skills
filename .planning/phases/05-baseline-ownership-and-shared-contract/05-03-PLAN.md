---
phase: 05-baseline-ownership-and-shared-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - skills/cloud-native-readiness/SKILL.md
  - skills/dockerfile-skill/SKILL.md
  - skills/docker-to-sealos/SKILL.md
  - skills/sealos-app-builder/SKILL.md
  - skills/sealos-database/SKILL.md
  - skills/sealos-s3/SKILL.md
  - skills/sealos-canvas/SKILL.md
  - skills/sealos-deploy/SKILL.md
  - docs/skill-runtime-preservation-checklist.md
autonomous: true
requirements:
  - SDS-01
  - SDS-03
  - SDS-05
must_haves:
  truths:
    - "All eight canonical entry files expose the shared core contract before branch-specific detail while retaining their domain-owned workflow and evidence."
    - "Every skill declares request-scoped success, stopped, and error outcomes; Canvas explicitly describes the loopback server lifetime and shutdown condition."
    - "Entry-visible safety canaries cover each applicable confirmation, redaction, kubeconfig, read-only, eligibility, quality-gate, and runtime-acceptance guard before detail links load."
    - "A preservation checklist ties every refactored entry to its baseline traces, owned artifacts, handoffs, and existing deterministic runtime gates."
  artifacts:
    - path: "skills/*/SKILL.md"
      provides: "Eight canonical entries implementing the shared contract skeleton and safety canaries"
    - path: "docs/skill-runtime-preservation-checklist.md"
      provides: "Eight-skill before/after preservation crosswalk and executed gate record"
  key_links:
    - from: "docs/skill-design-system.md"
      to: "skills/*/SKILL.md"
      via: "Each entry uses the exact ordered core before its domain extensions"
    - from: "docs/skill-safety-canaries.md"
      to: "skills/*/SKILL.md"
      via: "Canary IDs and markers remain visible at entry level with trigger and evidence text"
    - from: "tests/fixtures/skill-design-baseline.json"
      to: "docs/skill-runtime-preservation-checklist.md"
      via: "Checklist rows cite positive and violating baseline case IDs and compare terminal evidence"
    - from: "skills/sealos-deploy/SKILL.md"
      to: "skills/cloud-native-readiness/SKILL.md, skills/dockerfile-skill/SKILL.md, skills/docker-to-sealos/SKILL.md, skills/sealos-canvas/SKILL.md"
      via: "Composite handoffs retain named inputs, owners, allowed actions, failure returns, and Runtime Truth/canvas evidence"
---

<objective>
Apply the shared contract skeleton and entry-visible safety set to all eight canonical skills, then record runtime-preservation approval.

Purpose: Deliver SDS-01, SDS-03, and SDS-05 while preserving existing domain behavior, artifacts, handoffs, and safety semantics under D-01 through D-12.
Output: Eight focused entry contracts and a checklist proving their current behavior remains anchored to baseline traces and existing gates.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-CONTEXT.md
@docs/skill-design-baseline.md
@docs/skill-design-system.md
@docs/skill-safety-canaries.md
@tests/fixtures/skill-design-baseline.json
@scripts/skill-design-baseline.mjs
@scripts/test-skill-design-baseline.mjs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the contract to dependency and adjacent local-workflow entries</name>
  <files>skills/cloud-native-readiness/SKILL.md, skills/dockerfile-skill/SKILL.md, skills/docker-to-sealos/SKILL.md, skills/sealos-app-builder/SKILL.md</files>
  <action>Refactor each entry around the exact core order in `docs/skill-design-system.md`: identity/discovery, scope/boundaries, risk/confirmation, lifecycle workflow, progressive disclosure, output/stop/error states, handoffs, and verification. Keep current frontmatter names and invocation aliases. For `cloud-native-readiness`, make analysis-only eligibility, score gating, fail-closed stops, report evidence, and the Dockerfile handoff explicit before module links. For `dockerfile-skill`, declare owned local files, pre-existing-file handling, secret treatment, build acceptance, runtime/migration/HTTP/log evidence, and success/stopped/error outputs before existing analysis/generation detail. For `docker-to-sealos`, retain governance priority, condensed MUST rules, `references/must-rules-map.yaml` and `references/rules-registry.yaml` coupling, complete quality-gate requirement, template artifact output, and deploy handoff while routing detailed families through one owned reference level. For `sealos-app-builder`, declare create/adapt/identity/tutorial boundaries, local SDK/source precedence, client-only integration, real Desktop iframe verification, fallback behavior, and publish handoff. Preserve all existing domain extension text, helper names, file paths, and runtime semantics per D-02, D-07, D-08, and D-09.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; files=['skills/cloud-native-readiness/SKILL.md','skills/dockerfile-skill/SKILL.md','skills/docker-to-sealos/SKILL.md','skills/sealos-app-builder/SKILL.md']; headings=['## Identity and Discovery','## Scope and Boundaries','## Risk and Confirmation','## Lifecycle Workflow','## Progressive Disclosure','## Output, Stop, and Error States','## Handoffs','## Verification']; assert all(all(h in Path(f).read_text() for h in headings) for f in files); assert 'MUST-map' in Path('skills/docker-to-sealos/SKILL.md').read_text() or 'must-rules-map' in Path('skills/docker-to-sealos/SKILL.md').read_text()"</automated>
  </verify>
  <done>The four dependency/adjacent entries expose the shared contract first, keep their domain-specific workflows and links intact, and retain readiness, build/runtime, MUST-map/quality-gate, SDK, iframe, and publish safeguards.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the contract to service, Canvas, and deploy entries</name>
  <files>skills/sealos-database/SKILL.md, skills/sealos-s3/SKILL.md, skills/sealos-canvas/SKILL.md, skills/sealos-deploy/SKILL.md</files>
  <action>Apply the same eight core sections and canary IDs before branch detail. In `sealos-database`, retain analyzer-first create-or-reuse decisions, env-key preservation, secret redaction, public/destructive confirmation, connectivity evidence, and direct/deploy handoff. In `sealos-s3`, retain private-by-default buckets, create-or-reuse, env and credential boundaries, public policy and rotation confirmation, authenticated object-flow evidence, and cleanup output. In `sealos-canvas`, keep the read-only boundary, `.sealos/state.json` and Sealos kubeconfig preconditions, sanitized Secret/ConfigMap metadata, single generator call, `local_url`/cache output, and the temporary `127.0.0.1` server lifetime plus shutdown condition from D-06. In `sealos-deploy`, keep preflight/auth/workspace and kubeconfig scope, system-tool installation confirmation, deletion/public exposure/credential gates, one log file and `.sealos` artifact inventory, eligibility stop, dependency handoffs, quality-gate requirements, actual App URL/live identity, Runtime Truth, rollback/cleanup evidence, and all current phase order. Keep `AGENTS.md` branch policy authoritative and preserve existing module/script links per D-03, D-07, D-08, D-09, and D-12.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; files=['skills/sealos-database/SKILL.md','skills/sealos-s3/SKILL.md','skills/sealos-canvas/SKILL.md','skills/sealos-deploy/SKILL.md']; headings=['## Identity and Discovery','## Scope and Boundaries','## Risk and Confirmation','## Lifecycle Workflow','## Progressive Disclosure','## Output, Stop, and Error States','## Handoffs','## Verification']; assert all(all(h in Path(f).read_text() for h in headings) for f in files); c=Path('skills/sealos-canvas/SKILL.md').read_text(); assert all(k in c for k in ['127.0.0.1','local_url','read-only','Stop the server']); d=Path('skills/sealos-deploy/SKILL.md').read_text(); assert all(k in d for k in ['KUBECONFIG','Runtime Truth','confirmation','redact'])"</automated>
  </verify>
  <done>The four service/composite entries expose lifecycle and entry-visible safety before detail, Canvas states its local-server lifetime, and deploy/database/S3 behavior retains confirmation, redaction, artifact, handoff, and runtime-evidence contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Approve the runtime-preservation crosswalk and run existing gates</name>
  <files>docs/skill-runtime-preservation-checklist.md</files>
  <action>Write a checklist with one row per skill linking its core contract sections, canary IDs, positive and violating baseline case IDs, owned artifacts, handoff fields, terminal evidence, and existing deterministic gate commands. Include explicit preservation rows for readiness fail-closed eligibility, Dockerfile runtime acceptance, Docker-to-Sealos MUST-map/registry and quality gate, deploy auth/kubeconfig/confirmation/cleanup/Runtime Truth, database/S3 env and secret handling, Canvas read-only/sanitized output/server shutdown, and App Builder SDK/iframe/publish behavior. Mark a row approved only after its fixture and relevant current gate pass. Run the baseline checker/tests, `scripts/validate-codex-plugin.py`, the Docker-to-Sealos consistency/MUST/Compose test files, and the deploy footprint/live-smoke test files; record command names and pass evidence without recording secrets or live provider data. Keep provider smoke and host projection repair scoped to later phases while retaining all D-11 runtime-preservation oracles.</action>
  <verify>
    <automated>node scripts/skill-design-baseline.mjs --fixture tests/fixtures/skill-design-baseline.json --check && node --test scripts/test-skill-design-baseline.mjs && python3 scripts/validate-codex-plugin.py && python3 skills/docker-to-sealos/scripts/test_check_consistency.py && python3 skills/docker-to-sealos/scripts/test_check_must_coverage.py && python3 skills/docker-to-sealos/scripts/test_compose_to_template.py && node skills/sealos-deploy/scripts/test-sealos-footprint.mjs && node skills/sealos-deploy/scripts/test-sealos-live-smoke.mjs && git diff --check</automated>
  </verify>
  <done>The checklist records approved preservation evidence for all eight entries, all offline baseline and existing helper gates pass, and the entry refactor leaves host projections, provider smoke, and branch-specific release work for their assigned phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill entry -> agent execution | The first loaded Markdown sections govern whether a mutation, stop, or handoff can occur. |
| Entry -> owned modules/scripts | Progressive links expose detailed behavior while entry canaries remain load-bearing. |
| User request -> Sealos/Kubernetes mutation | Deploy, database, and S3 entries carry confirmation, scope, redaction, and runtime acceptance across this boundary. |
| Baseline/checklist -> preservation decision | Recorded traces and gate output determine whether an entry change preserves behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Elevation of Privilege | Deploy, database, S3 entry safety sections | critical | mitigate | Keep confirmation gates for destructive, public, credential, and system-tool actions visible before module links, with named evidence in the checklist. |
| T-05-08 | Information Disclosure | Entry output contracts and baseline/checklist docs | high | mitigate | Retain redaction rules for credentials, kubeconfig, env values, and connection strings; run baseline redaction checks and keep real values out of artifacts. |
| T-05-09 | Tampering | Docker-to-Sealos and deploy preservation seams | high | mitigate | Preserve MUST-map/registry, quality-gate, phase order, artifact inventory, Runtime Truth, and cleanup checks; run the existing deterministic helper tests before approval. |
| T-05-10 | Denial of Service | Canvas local server lifecycle | medium | mitigate | State loopback-only binding, request-scoped lifetime, and shutdown condition so the temporary process has an explicit terminal path. |
</threat_model>

<verification>
The Task 3 automated gate is the phase check. Review the checklist for eight approved rows, confirm every entry contains the shared core headings and applicable canary marker, and confirm `git diff --check` is clean.
</verification>

<success_criteria>
- All eight `SKILL.md` files contain the ordered core contract before domain-specific details.
- Every entry declares request-scoped success/stopped/error outcomes and its applicable entry-visible safety canaries; Canvas declares local-server lifetime and shutdown.
- The preservation checklist maps baseline evidence to existing runtime gates and records passing offline verification for all eight skills.
</success_criteria>

<output>
Create `.planning/phases/05-baseline-ownership-and-shared-contract/05-03-SUMMARY.md` when done.
</output>
