---
phase: 08-dependency-skill-entry-refactors
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - skills/docker-to-sealos/SKILL.md
  - skills/docker-to-sealos/references/must-rules-map.yaml
  - skills/docker-to-sealos/references/rules-registry.yaml
autonomous: true
requirements:
  - SDS-06
  - SDS-08
must_haves:
  truths:
    - "Compose conversion keeps the explicit rule precedence and one-level reference boundary visible while preserving the registry/MUST-map coupling."
    - "Topology, storage, database, secret, official-runtime, and image safeguards remain part of the conversion contract; KubeBlocks remains the database resource path."
    - "A template is handed to deploy only after consistency, MUST coverage, registry, topology, and quality-gate evidence pass, including the preview branch's Railpack/Kaniko adaptation."
  artifacts:
    - path: "skills/docker-to-sealos/SKILL.md"
      provides: "Conversion ownership, precedence, terminal evidence, and quality-gate handoff"
    - path: "skills/docker-to-sealos/references/must-rules-map.yaml"
      provides: "Machine-mapped MUST coverage source"
    - path: "skills/docker-to-sealos/references/rules-registry.yaml"
      provides: "Registry-driven rule source coupled to the entry"
  key_links:
    - from: "skills/docker-to-sealos/SKILL.md"
      to: "skills/docker-to-sealos/references/must-rules-map.yaml"
      via: "Entry MUST rules and one-level validation reference"
    - from: "skills/docker-to-sealos/SKILL.md"
      to: "skills/docker-to-sealos/scripts/quality_gate.py"
      via: "Complete quality-gate handoff boundary"
    - from: "skills/docker-to-sealos/SKILL.md"
      to: "skills/sealos-deploy/SKILL.md"
      via: "Validated Template YAML and evidence payload"

---

<objective>
Refocus Compose conversion around source precedence, topology-preserving artifacts, and an all-gates-passed deploy handoff.

Purpose: Satisfy the conversion half of SDS-06/SDS-08 while protecting the load-bearing Sealos rules and the preview branch's prepare-only behavior.
Output: A focused conversion entry and any narrowly necessary reference corrections with preserved registry semantics.
</objective>

<read_first>
  <file>.planning/phases/08-dependency-skill-entry-refactors/08-CONTEXT.md</file>
  <file>AGENTS.md</file>
  <file>skills/docker-to-sealos/SKILL.md</file>
  <file>skills/docker-to-sealos/references/must-rules-map.yaml</file>
  <file>skills/docker-to-sealos/references/rules-registry.yaml</file>
  <file>skills/docker-to-sealos/scripts/check_consistency.py</file>
  <file>skills/docker-to-sealos/scripts/check_must_coverage.py</file>
  <file>skills/docker-to-sealos/scripts/quality_gate.py</file>
  <file>skills/docker-to-sealos/scripts/compose_to_template.py</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Lock conversion source precedence and one-level references</name>
  <files>skills/docker-to-sealos/SKILL.md, skills/docker-to-sealos/references/must-rules-map.yaml, skills/docker-to-sealos/references/rules-registry.yaml</files>
  <action>Keep the entry MUST rules first, followed by Sealos specs/database templates, then mappings/examples. Make the input precedence explicit for existing templates, Compose, official Kubernetes docs, README/config, Dockerfile, lockfiles, and normalized build evidence. State that raw Railpack JSON is not a source and that the preview branch consumes normalized `analysis.json.build_environment` while retaining the Dockerfile plus Kaniko boundary. Keep references one level deep and require repository-relative owned paths. Preserve registry/must-map identifiers and only correct drift discovered by the existing validators.</action>
  <verify>
    <automated>python3 skills/docker-to-sealos/scripts/check_must_coverage.py --skill skills/docker-to-sealos/SKILL.md --mapping skills/docker-to-sealos/references/must-rules-map.yaml --rules-file skills/docker-to-sealos/references/rules-registry.yaml</automated>
  </verify>
  <done>Precedence, reference depth, branch adaptation, and rule-source ownership are explicit and machine-valid.</done>
</task>
<task type="auto">
  <name>Task 2: Make template delivery and quality evidence explicit</name>
  <files>skills/docker-to-sealos/SKILL.md</files>
  <action>Define conversion input, inference, resource-plan, template, and validation payloads. Require topology/storage/database/secret/runtime evidence, concrete images, validator-only topology evidence, and final `template/<app>/index.yaml` output. State that KubeBlocks resources remain the database path and that managed storage/security constraints remain active. Emit success only after consistency, MUST-map, registry, topology, and quality-gate checks; emit stopped for missing input/confirmation and error with failed rule/artifact and recovery action. Encode the five typed deploy handoff fields and keep live deployment/runtime out of this skill.</action>
  <verify>
    <automated>python3 skills/docker-to-sealos/scripts/check_consistency.py --skill skills/docker-to-sealos/SKILL.md --references references --rules-file references/rules-registry.yaml</automated>
  </verify>
  <done>Conversion emits a deployable handoff only with complete quality-gate evidence and preserves existing artifact semantics.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-07 | Tampering | Rule precedence/registry | critical | mitigate | Keep entry MUST rules and registry/map coupling machine-checked. |
| T-08-08 | Integrity | Template topology/database | critical | mitigate | Preserve source topology, KubeBlocks database resources, and validator-only evidence boundaries. |
| T-08-09 | Execution | Partial template handoff | high | mitigate | Gate deploy handoff on consistency, MUST, registry, topology, and quality checks. |
| T-08-10 | Scope | Preview branch | critical | mitigate | Preserve normalized build evidence and Kaniko path; exclude Railpack build, BuildKit, and full runtime behavior. |
</threat_model>

<verification>
Run the targeted MUST and consistency checks first, then the complete `quality_gate.py` and existing converter test suites against the available artifacts or with the documented empty-artifact mode.
</verification>

<success_criteria>
SDS-06 and SDS-08 are advanced for Compose conversion: source precedence, rule ownership, topology safeguards, and all-gates-passed handoff are explicit without changing runtime behavior.
</success_criteria>

<output>
Create `.planning/phases/08-dependency-skill-entry-refactors/08-03-SUMMARY.md` after execution.
</output>
