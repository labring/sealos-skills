---
phase: 08-dependency-skill-entry-refactors
plan: 01
type: execute
wave: 1
depends_on:
  - 07-03
files_modified:
  - skills/cloud-native-readiness/SKILL.md
  - skills/cloud-native-readiness/modules/assess.md
  - skills/cloud-native-readiness/modules/detect.md
  - skills/cloud-native-readiness/modules/route.md
autonomous: true
requirements:
  - SDS-06
  - SDS-08
must_haves:
  truths:
    - "Readiness remains read-only and stops unsupported or unresolved targets before scoring, artifact detection, Dockerfile generation, or deployment."
    - "Eligible output carries a minimal typed report with source, workload, eligibility, score dimensions, artifact inventory, concerns, recommendation, and redaction evidence."
    - "The readiness-to-Dockerfile branch exposes all five handoff fields and does not duplicate Dockerfile behavior."
  artifacts:
    - path: "skills/cloud-native-readiness/SKILL.md"
      provides: "Eligibility-first entry contract, report schema, terminal states, and typed handoff"
    - path: "skills/cloud-native-readiness/modules/route.md"
      provides: "Eligibility-gated decision matrix and report/handoff routing"
  key_links:
    - from: "skills/cloud-native-readiness/SKILL.md"
      to: "skills/cloud-native-readiness/knowledge/deployment-eligibility.md"
      via: "One-level conditional eligibility load before scoring"
    - from: "skills/cloud-native-readiness/modules/route.md"
      to: "skills/dockerfile-skill/SKILL.md"
      via: "Typed readiness report handoff"

---

<objective>
Refocus the readiness entry around a fail-closed eligibility decision and a small downstream payload while preserving the existing scorer, detector, and route behavior.

Purpose: Satisfy the readiness portion of SDS-06/SDS-08 and give Dockerfile work actionable evidence without repeated repository discovery.
Output: A concise entry/module contract with explicit stopped/error branches and a typed readiness report.
</objective>

<read_first>
  <file>.planning/phases/08-dependency-skill-entry-refactors/08-CONTEXT.md</file>
  <file>.planning/ROADMAP.md</file>
  <file>.planning/REQUIREMENTS.md</file>
  <file>docs/skill-design-system.md</file>
  <file>skills/cloud-native-readiness/SKILL.md</file>
  <file>skills/cloud-native-readiness/modules/assess.md</file>
  <file>skills/cloud-native-readiness/modules/detect.md</file>
  <file>skills/cloud-native-readiness/modules/route.md</file>
  <file>skills/cloud-native-readiness/knowledge/deployment-eligibility.md</file>
  <file>tests/fixtures/skill-design-baseline.json</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Tighten eligibility-first entry contract</name>
  <files>skills/cloud-native-readiness/SKILL.md, skills/cloud-native-readiness/modules/assess.md</files>
  <action>Keep the shared core sections entry-visible and make the first decision an eligibility read. State that unsupported and unresolved workload types return stopped with workload, reason codes, observed evidence, safe next action, and redaction status; they must not score, detect artifacts, or route. Define the eligible report fields and preserve the current six-dimension 0-12 scoring model and helper ownership. Keep source paths, env values, and credentials redacted. Point to the eligibility knowledge and assessment module at one level only.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; text=Path('skills/cloud-native-readiness/SKILL.md').read_text(); required=['CNR-ELIGIBILITY-STOP','eligibility','stopped','redacted','score','dimensions','artifact','recommendation']; assert all(token in text for token in required)"</automated>
  </verify>
  <done>The entry visibly defines fail-closed eligibility, minimal report evidence, and preserved score ownership.</done>
</task>
<task type="auto">
  <name>Task 2: Make route payload and branch ordering explicit</name>
  <files>skills/cloud-native-readiness/modules/detect.md, skills/cloud-native-readiness/modules/route.md</files>
  <action>Rewrite the route contract around an explicit input payload containing eligibility status, workload type, assessment score/dimensions, artifact inventory, concerns, and redaction result. Enforce eligibility before detection and score use, preserve report-only branches for complete/partial artifacts, preserve the fair-score confirmation boundary, and route only the eligible no-artifact branch to Dockerfile. Encode the five handoff fields exactly, identify the response owner and failure return, and keep the receiving skill responsible for its own canaries. Remove duplicated Dockerfile procedure from the readiness entry.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; text=Path('skills/cloud-native-readiness/modules/route.md').read_text(); required=['eligibility_status','assessment_score','artifacts_status','target: dockerfile-skill','inputArtifact','allowedAction','failureReturn','responseOwner']; assert all(token in text for token in required); assert text.index('eligibility') < text.index('artifact')"</automated>
  </verify>
  <done>Every readiness branch has an observable terminal result and only the eligible packaging branch emits a typed Dockerfile handoff.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Elevation of Privilege | Eligibility bypass | critical | mitigate | Require eligibility before score, detection, or downstream route and fixture-test the stopped branch. |
| T-08-02 | Information Disclosure | Readiness report | high | mitigate | Keep source, env, credential-shaped, and connection values redacted in every terminal state. |
| T-08-03 | Integrity | Handoff payload | high | mitigate | Require five typed fields plus provenance and failed-route ownership before Dockerfile routing. |
</threat_model>

<verification>
Run the focused text contract checks, then the Phase 5/6/7 baseline and aggregate validators after the dependent plans land. Preserve the existing readiness eval IDs and helper behavior.
</verification>

<success_criteria>
SDS-06 and SDS-08 are advanced for readiness: one-level detail is resolvable, eligibility is fail-closed, report evidence is minimal and typed, and Dockerfile routing does not repeat discovery.
</success_criteria>

<output>
Create `.planning/phases/08-dependency-skill-entry-refactors/08-01-SUMMARY.md` after execution.
</output>
