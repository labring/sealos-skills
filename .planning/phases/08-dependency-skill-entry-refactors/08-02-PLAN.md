---
phase: 08-dependency-skill-entry-refactors
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - skills/dockerfile-skill/SKILL.md
  - skills/dockerfile-skill/modules/analyze.md
  - skills/dockerfile-skill/modules/generate.md
  - skills/dockerfile-skill/modules/build-fix.md
autonomous: true
requirements:
  - SDS-06
  - SDS-08
must_haves:
  truths:
    - "Dockerfile work has a named owned-file scope and preserves pre-existing packaging files unless replacement is explicitly authorized."
    - "A successful build alone cannot produce success; migration/database, HTTP/health, and runtime-log evidence are required."
    - "The deploy handoff carries a validated Dockerfile plus build/runtime evidence and keeps secrets, environment values, and connection strings redacted."
  artifacts:
    - path: "skills/dockerfile-skill/SKILL.md"
      provides: "Owned-file, confirmation, runtime acceptance, terminal output, and deploy handoff contract"
    - path: "skills/dockerfile-skill/modules/analyze.md"
      provides: "Input provenance, workspace analysis, and file-scope decision"
    - path: "skills/dockerfile-skill/modules/generate.md"
      provides: "Explicit packaging mutation plan and pre-existing-file policy"
    - path: "skills/dockerfile-skill/modules/build-fix.md"
      provides: "Closed-loop build and runtime acceptance evidence"
  key_links:
    - from: "skills/cloud-native-readiness/modules/route.md"
      to: "skills/dockerfile-skill/modules/analyze.md"
      via: "Readiness report as input provenance"
    - from: "skills/dockerfile-skill/modules/build-fix.md"
      to: "skills/sealos-deploy/SKILL.md"
      via: "Validated packaging and build/runtime result handoff"

---

<objective>
Refocus Dockerfile generation around controlled file ownership and runtime acceptance, keeping existing generation/build helpers and the current four-phase behavior.

Purpose: Make packaging mutations reviewable and make runtime evidence the acceptance boundary before deploy consumes the result.
Output: Entry and module contracts that distinguish analysis, generation, build/fix, and runtime validation without duplicating deploy behavior.
</objective>

<read_first>
  <file>.planning/phases/08-dependency-skill-entry-refactors/08-CONTEXT.md</file>
  <file>skills/dockerfile-skill/SKILL.md</file>
  <file>skills/dockerfile-skill/modules/analyze.md</file>
  <file>skills/dockerfile-skill/modules/generate.md</file>
  <file>skills/dockerfile-skill/modules/build-fix.md</file>
  <file>skills/dockerfile-skill/scripts/validate-dockerfile.mjs</file>
  <file>tests/fixtures/skill-design-baseline.json</file>
  <file>docs/skill-safety-canaries.md</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Define packaging ownership and analysis payload</name>
  <files>skills/dockerfile-skill/SKILL.md, skills/dockerfile-skill/modules/analyze.md</files>
  <action>Keep the four-phase workflow and canaries visible while defining an analysis payload with source, readiness provenance when present, detected framework/package manager, workspace boundaries, migration/runtime concerns, candidate output files, and redaction status. State the named file set and default preservation policy for existing Dockerfile, .dockerignore, compose, entrypoint, and docs. Require a pre-mutation decision for replacement and keep system-tool installation under existing confirmation semantics. Keep one-level links to analyze/generate/build-fix and owned knowledge/templates.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; text=Path('skills/dockerfile-skill/SKILL.md').read_text(); required=['DFS-OWNED-FILES','DFS-REDACT','pre-existing','Dockerfile','stopped','error','redacted']; assert all(token in text for token in required)"</automated>
  </verify>
  <done>The entry and analysis module make source provenance, file scope, preservation, and confirmation decisions explicit before writes.</done>
</task>
<task type="auto">
  <name>Task 2: Make runtime acceptance and deploy handoff deterministic</name>
  <files>skills/dockerfile-skill/modules/generate.md, skills/dockerfile-skill/modules/build-fix.md</files>
  <action>Define generated artifact and build/runtime result fields: named files, Dockerfile validation, image/build identity, migration/database proof, HTTP/health proof, log scan, redaction result, terminal state, failed step, and safe recovery action. Treat build-only or missing runtime evidence as a non-success result. Preserve iterative error-pattern fixes, migration handling, and helper commands. Emit the five typed deploy handoff fields with `inputArtifact` naming the validated Dockerfile and build/runtime result, while leaving image build/reuse and Runtime Truth to deploy.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; text=Path('skills/dockerfile-skill/modules/build-fix.md').read_text(); required=['migration','HTTP','health','log','build-only','inputArtifact','allowedAction','failureReturn','responseOwner']; assert all(token.lower() in text.lower() for token in required)"</automated>
  </verify>
  <done>Dockerfile success is evidence-backed and the deploy boundary is typed, scoped, and redaction-safe.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04 | Integrity | Pre-existing packaging files | high | mitigate | Record named ownership and require explicit replacement authorization. |
| T-08-05 | Availability | Build accepted without runtime proof | critical | mitigate | Require migration, HTTP/health, and log evidence before success. |
| T-08-06 | Information Disclosure | Build/runtime diagnostics | high | mitigate | Keep secrets, env values, URLs, and credentials redacted in artifacts and output. |
</threat_model>

<verification>
Run `node --check skills/dockerfile-skill/scripts/validate-dockerfile.mjs` and the entry contract checks, then preserve the baseline positive/violating Dockerfile traces and any existing helper test command.
</verification>

<success_criteria>
SDS-06 and SDS-08 are advanced for Dockerfile work: writes are scoped, runtime acceptance is explicit, and deploy receives a typed validated artifact without repeated discovery.
</success_criteria>

<output>
Create `.planning/phases/08-dependency-skill-entry-refactors/08-02-SUMMARY.md` after execution.
</output>
