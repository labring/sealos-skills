---
phase: 06-inventory-router-and-validator-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - scripts/skill_design_safety.py
  - scripts/test_skill_design_safety.py
  - tests/fixtures/skill-design-safety.json
autonomous: true
requirements:
  - SDS-D04
must_haves:
  truths:
    - "Every Phase 5 safety canary is evaluated against its owning entry and required evidence phrases, with stable diagnostics naming the canary and owner."
    - "Each confirmation, redaction, read-only, eligibility-stop, and fail-closed guard has a passing live fixture and a deterministic failing mutation fixture."
    - "Equivalent casing, punctuation, and whitespace preserve a valid semantic guard while removal of the guard or its required evidence fails targeted validation."
  artifacts:
    - path: "scripts/skill_design_safety.py"
      provides: "Canary registry reader, normalized marker/evidence checker, and mutation probe CLI"
    - path: "scripts/test_skill_design_safety.py"
      provides: "Offline safety-canary unittest suite with positive and violating mutations"
    - path: "tests/fixtures/skill-design-safety.json"
      provides: "Deterministic mutation cases linked to the Phase 5 canary IDs"
  key_links:
    - from: "scripts/skill_design_safety.py"
      to: "docs/skill-safety-canaries.md"
      via: "Registry rows supply canary IDs, owners, markers, triggers, and required evidence"
    - from: "scripts/skill_design_safety.py"
      to: "skills/*/SKILL.md"
      via: "Canary probes inspect entry-visible policy text before detail links"
---

<objective>
Add fixture-tested semantic-safety canaries and targeted mutation probes for the eight canonical skill entries.

Purpose: Deliver SDS-D04 by proving that operational safety survives equivalent wording and that removal of a load-bearing guard fails even when unrelated entry prose remains valid.
Output: A standard-library safety checker, a mutation fixture set, and red/green tests covering confirmation, redaction, read-only, eligibility, and fail-closed behavior.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory-router-and-validator-foundation/06-CONTEXT.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-02-SUMMARY.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-03-SUMMARY.md
@docs/skill-safety-canaries.md
@tests/fixtures/skill-design-baseline.json
@scripts/skill_design_inventory.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the canary checker and mutation fixture schema</name>
  <files>scripts/skill_design_safety.py, tests/fixtures/skill-design-safety.json</files>
  <action>Implement `scripts/skill_design_safety.py` as an importable standard-library checker that reads the Phase 5 canary registry from `docs/skill-safety-canaries.md`, resolves each owning `skills/<name>/SKILL.md` through the Plan 06-01 constrained path reader, and checks the entry-visible marker plus every registry-required evidence phrase. Per D-07, use canary IDs and owning entries as the diagnostic identity; per D-08, require one positive and one violating mutation case for every applicable canary; per D-09, keep probes text-based, deterministic, offline, and provider-free. Normalize case, punctuation, whitespace, and the registry's declared equivalent wording so semantically equivalent guards pass. Define `tests/fixtures/skill-design-safety.json` with schema version 1, one mutation record per canary, stable mutation operation, expected diagnostic code, and expected terminal guard category; derive skill membership from the registry and physical inventory rather than adding a second inventory list. Expose `check_safety` and a `--fixture ... --check` CLI that reports `canary.missing`, `canary.evidence_missing`, `canary.owner_missing`, or `canary.fixture_malformed` with source path and canary ID.
  <verify>
    <automated>python3 scripts/skill_design_safety.py --root . --fixture tests/fixtures/skill-design-safety.json --check</automated>
  </verify>
  <done>The checker loads all Phase 5 canaries, validates live entry markers and evidence, accepts normalized equivalent guards, and produces targeted diagnostics for missing policy, missing evidence, bad owner, or malformed mutation records.</done>
</task>

<task type="auto">
  <name>Task 2: Prove red/green semantic mutations across safety classes</name>
  <files>scripts/test_skill_design_safety.py</files>
  <action>Write `unittest` coverage that creates temporary repository copies and applies one isolated mutation per canary: remove confirmation language for gated mutations, remove redaction evidence, add a forbidden Canvas mutation verb, remove readiness eligibility-stop evidence, and remove the fail-closed stop before scoring/build/deploy. Assert each live fixture passes, each mutated fixture fails with the declared canary ID, owner path, and category-specific diagnostic, and unrelated prose remains accepted. Include tests for malformed fixture records, duplicate canary IDs, unknown owners, missing registry rows, and registry evidence phrases that differ only by case or punctuation. Exercise both `check_safety` as an importable API and the CLI, then run alongside the Plan 06-01 inventory tests without network calls or package installation.
  <verify>
    <automated>python3 scripts/test_skill_design_safety.py && python3 scripts/skill_design_inventory.py --root . --router commands/sealos.md --check</automated>
  </verify>
  <done>All live canaries pass, every required semantic mutation is rejected with an actionable canary-specific failure, and the complete offline safety suite remains deterministic and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canary registry -> semantic checker | Maintainer-authored markers and evidence define which safety promises are enforceable. |
| Skill entry text -> mutation probe | Entry text controls confirmation, redaction, read-only, eligibility, and fail-closed behavior observed by the checker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | Confirmation, eligibility, and fail-closed canaries | critical | mitigate | Pair every applicable canary with a positive fixture and a guard-removal mutation; fail with the stable canary ID before any runtime action. |
| T-06-05 | Information Disclosure | Redaction canaries and evidence probes | high | mitigate | Require registry evidence for sanitized credentials, kubeconfig, env values, and connection data; test normalized wording and explicit removal. |
| T-06-06 | Tampering | Canvas read-only canaries | high | mitigate | Reject mutation verbs and missing read-only evidence while preserving the entry's existing sanitized output and deployed-state preconditions. |
</threat_model>

<verification>
Run the safety checker and semantic mutation suite, then rerun the inventory CLI. Review that every Phase 5 canary maps to a positive live fixture and a failing mutation with an exact source path and diagnostic ID.
</verification>

<success_criteria>
SDS-D04 is covered by deterministic static canaries and behavior probes for confirmation, redaction, read-only, eligibility-stop, and fail-closed guards across the eight canonical entries.
</success_criteria>

<output>
Create `.planning/phases/06-inventory-router-and-validator-foundation/06-02-SUMMARY.md` when done.
</output>
