---
phase: 06-inventory-router-and-validator-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - scripts/validate_skill_design.py
  - scripts/test_validate_skill_design.py
autonomous: true
requirements:
  - SDS-09
  - SDS-D04
must_haves:
  truths:
    - "One companion design validator reports inventory, projection, route, frontmatter, version, path, link, canary, and eval-schema drift with exact file and field diagnostics."
    - "The canonical version comes from .codex-plugin/plugin.json and every version-bearing projection is compared against it without a duplicate version source."
    - "Existing scripts/validate-codex-plugin.py remains callable while the new validator uses the physical skill tree and broad router as its authoritative sources."
  artifacts:
    - path: "scripts/validate_skill_design.py"
      provides: "Importable and CLI structural, semantic, metadata, link, and eval-schema validator"
    - path: "scripts/test_validate_skill_design.py"
      provides: "Offline unittest mutations for every Phase 6 diagnostic category"
  key_links:
    - from: "scripts/validate_skill_design.py"
      to: "scripts/skill_design_inventory.py"
      via: "Validator consumes the derived inventory, route records, frontmatter, and constrained path diagnostics"
    - from: "scripts/validate_skill_design.py"
      to: "scripts/skill_design_safety.py"
      via: "Validator includes canary checks and preserves safety diagnostics in the aggregate report"
    - from: "scripts/validate_skill_design.py"
      to: ".codex-plugin/plugin.json"
      via: "Version-bearing projection checks load one canonical package version"
---

<objective>
Assemble the Phase 6 inventory, router, safety, metadata, link, and eval readers into one focused companion validator with fixture-driven diagnostics.

Purpose: Give Phase 7 and Phase 11 a reusable foundation that exposes structural and semantic drift while preserving the existing Codex plugin validator CLI.
Output: `validate_skill_design.py`, its offline unittest suite, and a documented command surface ready for later host repair and maintainer-gate consolidation.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory-router-and-validator-foundation/06-CONTEXT.md
@.planning/phases/06-inventory-router-and-validator-foundation/06-01-PLAN.md
@.planning/phases/06-inventory-router-and-validator-foundation/06-02-PLAN.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-01-SUMMARY.md
@docs/skill-design-system.md
@docs/skill-safety-canaries.md
@tests/fixtures/skill-design-baseline.json
@scripts/validate-codex-plugin.py
@scripts/skill_design_inventory.py
@scripts/skill_design_safety.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the aggregate design-system validator</name>
  <files>scripts/validate_skill_design.py</files>
  <read_first>
    <file>scripts/skill_design_inventory.py</file>
    <file>scripts/skill_design_safety.py</file>
    <file>scripts/validate-codex-plugin.py</file>
    <file>.codex-plugin/plugin.json</file>
    <file>docs/skill-safety-canaries.md</file>
    <file>tests/fixtures/skill-design-baseline.json</file>
    <file>.planning/phases/06-inventory-router-and-validator-foundation/06-CONTEXT.md</file>
  </read_first>
  <action>Build a dependency-free companion validator that imports the Plan 06-01 inventory/route readers and Plan 06-02 safety checker, exposes `validate_design_system` for synthetic repositories, and provides `python3 scripts/validate_skill_design.py --root . --check`. Per D-03, compare every explicit host projection against the derived eight-skill set and report missing, unexpected, duplicate, and malformed projections while preserving the current Canvas omission as a concrete failure for Phase 7. Per D-10, load `.codex-plugin/plugin.json.version` as the sole canonical version and compare all version-bearing fields in root/Codex/Claude/Qoder/CodeBuddy marketplaces, Gemini, Qwen, OpenClaw, and `distribution/platforms.json`; honor versionless `.agents/plugins/marketplace.json` semantics and, when `GITHUB_REF_NAME` is present, compare its leading-v-stripped value to the same canonical version. Per D-11, resolve every Markdown relative link, `<SKILL_DIR>` reference, route target, and skill-owned resource one level deep through the constrained reader, and validate every present `skills/*/evals/evals.json` record against the Phase 5 trace shape with unique IDs/assertions, non-empty prompts and expected outputs, and design tags. Emit stable codes including `inventory.missing_projection`, `inventory.unexpected_projection`, `route.missing_skill`, `frontmatter.name_mismatch`, `path.outside_root`, `link.broken`, `version.mismatch`, `canary.missing`, and `eval.malformed`; keep `scripts/validate-codex-plugin.py` untouched and callable per D-12. Keep the derived inventory and canonical package version as the sole sources.
  <acceptance_criteria>
    <criterion>`python3 scripts/validate_skill_design.py --root . --check` reports current Canvas projection drift with stable `inventory.missing_projection` diagnostics and exits non-zero until Phase 7 repairs it.</criterion>
    <criterion>The validator imports inventory and safety readers, derives version only from `.codex-plugin/plugin.json`, and emits exact file/field diagnostics for routes, paths, links, versions, canaries, and eval records.</criterion>
    <criterion>`python3 scripts/validate-codex-plugin.py` remains callable with its existing behavior.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/validate_skill_design.py --root . --check; python3 scripts/validate-codex-plugin.py</automated>
  </verify>
  <done>The companion validator aggregates inventory, route, constrained-path, frontmatter, host projection, canonical-version, link, canary, and eval diagnostics with source file and field context, while the existing Codex validator still executes.</done>
</task>

<task type="auto">
  <name>Task 2: Add full diagnostic mutation coverage</name>
  <files>scripts/test_validate_skill_design.py</files>
  <read_first>
    <file>scripts/test_validate_skill_design.py</file>
    <file>scripts/validate_skill_design.py</file>
    <file>scripts/skill_design_inventory.py</file>
    <file>scripts/skill_design_safety.py</file>
    <file>scripts/validate-codex-plugin.py</file>
  </read_first>
  <action>Write `unittest` fixtures with temporary repository copies that mutate one source at a time and assert the aggregate validator's exact diagnostic code, file, field, and target. Cover a missing or unexpected host skill projection, missing route, frontmatter name mismatch, outside-root link, broken progressive Markdown link, stale version in each projection family, absent or malformed canary evidence, malformed eval record, duplicate eval ID/assertion, and CRLF normalization. Include a passing fixture that runs all available inventory, route, safety, link, version, and eval checks against the live tree, and a regression assertion that invoking `python3 scripts/validate-codex-plugin.py` remains a supported command. Keep tests offline and standard-library only; preserve Phase 7's responsibility for repairing the current seven-versus-eight projections and Phase 11's responsibility for one consolidated maintainer gate.
  <acceptance_criteria>
    <criterion>`python3 scripts/test_validate_skill_design.py` passes green baseline and red mutation cases for every listed Phase 6 diagnostic category.</criterion>
    <criterion>Each mutation assertion checks stable code, source file, field, and target detail rather than only process exit status.</criterion>
    <criterion>`python3 scripts/validate_skill_design.py --root . --check` and `python3 scripts/validate-codex-plugin.py` both execute from the repository root.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/test_validate_skill_design.py && python3 scripts/validate_skill_design.py --root . --check</automated>
  </verify>
  <done>Every Phase 6 structural and semantic diagnostic has a deterministic red mutation and a green baseline, and downstream phases can consume the importable report without changing host behavior.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `scripts/validate_skill_design.py`: `DesignDiagnostic`, `validate_design_system`, projection/version/link/eval readers, and the `--check` CLI.
- `scripts/test_validate_skill_design.py`: standard-library aggregate-validator mutation suite.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host projections -> aggregate validator | Explicit manifests and platform claims are untrusted projections of the physical inventory. |
| Canonical version -> release metadata | A single package version controls all version-bearing JSON projections. |
| Markdown/eval fixtures -> diagnostics | Resolver-visible links, canaries, and eval records become maintainer gate input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | Host inventory and route projections | high | mitigate | Anchor comparisons to discovered physical entries and broad-router records; report exact missing, unexpected, duplicate, or malformed projection fields. |
| T-06-08 | Tampering | Version-bearing metadata | medium | mitigate | Load the version only from `.codex-plugin/plugin.json` and reject stale or conflicting projection values with `version.mismatch`. |
| T-06-09 | Information Disclosure | Eval and diagnostic output | high | mitigate | Reuse redaction-aware canary checks, keep diagnostics path/field scoped, and reject credential-shaped fixture values before reporting them. |
</threat_model>

<verification>
Run the aggregate validator and mutation suite, invoke the existing Codex plugin validator, and inspect the report for the current Canvas projection drift plus every required stable diagnostic code. Confirm `git diff --check` remains clean.
</verification>

<success_criteria>
The repository has an importable, fixture-tested Phase 6 design validator that covers inventory, routes, versions, links, canaries, and eval schema while retaining the existing Codex validator and deferring host repairs to Phase 7.
</success_criteria>

<output>
Create `.planning/phases/06-inventory-router-and-validator-foundation/06-03-SUMMARY.md` when done.
</output>
