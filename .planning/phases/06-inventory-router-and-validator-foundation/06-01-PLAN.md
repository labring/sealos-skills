---
phase: 06-inventory-router-and-validator-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sealos.md
  - scripts/skill_design_inventory.py
  - scripts/test_skill_design_inventory.py
autonomous: true
requirements:
  - SDS-09
must_haves:
  truths:
    - "The canonical inventory is discovered from exactly the immediate skills/*/SKILL.md entry files and each entry has a non-empty matching frontmatter name and description."
    - "The broad Sealos router contains one parseable route record for every physical skill, with stable display order and canonical set equality."
    - "Repository-owned Markdown and JSON references are rejected when they traverse outside the repository or point at missing targets."
  artifacts:
    - path: "scripts/skill_design_inventory.py"
      provides: "Importable and CLI inventory, frontmatter, route, and constrained-path readers"
    - path: "scripts/test_skill_design_inventory.py"
      provides: "Offline unittest fixtures for inventory and route drift"
    - path: "commands/sealos.md"
      provides: "Human-readable broad route table consumed as the single router source"
  key_links:
    - from: "scripts/skill_design_inventory.py"
      to: "skills/*/SKILL.md"
      via: "discover_inventory scans immediate skill directories and parses deterministic frontmatter"
    - from: "scripts/skill_design_inventory.py"
      to: "commands/sealos.md"
      via: "parse_router reads the canonical route table and compares skill-name sets"
---

<objective>
Implement the canonical physical inventory and the constrained broad-router readers that later Phase 6 validators consume.

Purpose: Make the eight `skills/*/SKILL.md` files and `commands/sealos.md` machine-checkable while preserving the existing host behavior and leaving host projection repair to Phase 7.
Output: A dependency-free Python reader module, a parseable eight-row route table, and red/green inventory/reader tests.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory-router-and-validator-foundation/06-CONTEXT.md
@.planning/phases/05-baseline-ownership-and-shared-contract/05-03-SUMMARY.md
@docs/skill-design-baseline.md
@docs/skill-design-system.md
@docs/skill-safety-canaries.md
@commands/sealos.md
@scripts/validate-codex-plugin.py
@scripts/skill-design-baseline.mjs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical inventory and route readers</name>
  <files>commands/sealos.md, scripts/skill_design_inventory.py</files>
  <action>Replace the prose route bullets in `commands/sealos.md` with one human-readable Markdown table under `## Route` using the columns `Intent`, `Canonical skill`, `Plugin entry`, and `Direct skills.sh entry`; retain the command frontmatter, safety rules, examples, and the existing seven-versus-eight host projection drift. Implement importable functions such as `discover_inventory`, `parse_skill_frontmatter`, `parse_router`, and `validate_repo_path` in `scripts/skill_design_inventory.py` using only the Python standard library. Per D-01, discover immediate `skills/*/SKILL.md` files and derive canonical names from validated non-empty `name` frontmatter plus `description`; per D-02, use router order only as display metadata while comparing sets for parity; per D-04, require one route record per canonical skill and all four route fields; per D-05, accept repository-relative POSIX paths, normalize separators, reject traversal, missing targets, absolute paths, and paths outside the supplied root; per D-06, parse the existing `---` frontmatter block deterministically and preserve Markdown content. Return structured diagnostics with stable codes and expose a `--check` CLI that exits non-zero on malformed inventory or routes.
  <verify>
    <automated>python3 scripts/skill_design_inventory.py --root . --router commands/sealos.md --check && python3 -c "from pathlib import Path; from scripts.skill_design_inventory import discover_inventory, parse_router; skills=discover_inventory(Path('.')); routes=parse_router(Path('commands/sealos.md')); assert len(skills)==8; assert {s.name for s in skills}=={r.skill for r in routes}; assert all(r.intent and r.plugin_entry and r.direct_entry for r in routes)"</automated>
  </verify>
  <done>`commands/sealos.md` has exactly eight route rows, the reader derives the same eight canonical names from physical entries, and malformed names, descriptions, routes, or paths produce actionable structured diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture-driven inventory and router regression tests</name>
  <files>scripts/test_skill_design_inventory.py</files>
  <action>Write `unittest` coverage using `tempfile.TemporaryDirectory` repositories that copy only the relevant source files and mutate one condition at a time. Per D-01 and D-03, prove the live tree discovers eight entries while synthetic host projection lists remain reports rather than inventory sources. Cover missing and unexpected skill directories, duplicate or missing route records, frontmatter `name`/`description` failures, malformed route columns, missing route targets, path traversal, absolute paths, and CRLF Markdown normalization. Assert diagnostics identify the stable code, source path, and affected canonical skill, and include a passing fixture that preserves the current Canvas omission in explicit host lists for the later projection phase. Keep tests importable from the reader module and avoid network, package installation, or edits to the live tree.
  <verify>
    <automated>python3 scripts/test_skill_design_inventory.py</automated>
  </verify>
  <done>Inventory and route readers have deterministic red/green tests for every Phase 6 structural failure class, including constrained-path and frontmatter validation, with all tests passing against the live eight-skill source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository Markdown/JSON -> parser | Maintainer-authored paths and frontmatter become executable validation input. |
| Router table -> skill selection | A malformed or duplicated route can select the wrong owner or hide a capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `skills/*/SKILL.md` inventory discovery | high | mitigate | Derive the set from immediate entry files, validate frontmatter identity, and fail on missing, duplicate, or unexpected entries with path-specific diagnostics. |
| T-06-02 | Elevation of Privilege | `commands/sealos.md` route table | high | mitigate | Require one canonical route per physical skill, validate all route fields and targets, and compare route sets against the derived inventory. |
| T-06-03 | Path Traversal | constrained Markdown/JSON readers | high | mitigate | Normalize repository-relative paths and reject absolute, parent-traversing, missing, or outside-root targets before opening them. |
</threat_model>

<verification>
Run `python3 scripts/skill_design_inventory.py --root . --router commands/sealos.md --check` and `python3 scripts/test_skill_design_inventory.py`; confirm the live result reports eight physical entries and eight route records while synthetic mutations fail with stable diagnostics.
</verification>

<success_criteria>
The repository has one derived eight-skill inventory, one parseable broad router, deterministic constrained readers, and passing structural regression tests ready for the semantic and metadata validators in Plans 06-02 and 06-03.
</success_criteria>

<output>
Create `.planning/phases/06-inventory-router-and-validator-foundation/06-01-SUMMARY.md` when done.
</output>
