---
phase: 10-deploy-orchestration-and-runtime-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sealos-deploy/SKILL.md
  - skills/sealos-deploy/references/deploy-contract.md
  - skills/sealos-deploy/schemas/deploy-handoff.schema.json
  - scripts/test_deploy_entry_contract.mjs
autonomous: true
requirements:
  - SDS-D01
  - SDS-D03
must_haves:
  truths:
    - "The deploy entry exposes the composite phase order, typed evidence boundary, and terminal output contract before module detail."
    - "A deploy handoff can be validated as a source/owner/precondition/evidence envelope with the five stable fields and redaction status."
    - "The entry preserves the full-deploy main workflow and names the prepare-only preview boundary without importing preview behavior."
  artifacts:
    - path: skills/sealos-deploy/references/deploy-contract.md
      provides: "Typed deploy envelope, phase ownership, artifact inventory, and output contract"
    - path: skills/sealos-deploy/schemas/deploy-handoff.schema.json
      provides: "Machine-readable handoff envelope validation"
    - path: scripts/test_deploy_entry_contract.mjs
      provides: "Provider-free entry contract and schema tests"
  key_links:
    - from: skills/sealos-deploy/SKILL.md
      to: skills/sealos-deploy/references/deploy-contract.md
      via: "Entry-visible conditional reference after preflight canaries"
    - from: skills/sealos-deploy/references/deploy-contract.md
      to: skills/sealos-deploy/schemas/deploy-handoff.schema.json
      via: "Typed envelope fields and terminal evidence map"
---

<objective>
Refocus the deploy entry around a typed composite phase map, stable terminal outcomes, and owned `.sealos` evidence without changing execution behavior.

Purpose: Give downstream plans a single contract boundary for consuming Phase 8 and 9 evidence.
Output: Entry-visible deploy contract, handoff schema, and deterministic contract tests.
</objective>

<read_first>
  <file>.planning/phases/10-deploy-orchestration-and-runtime-truth/10-CONTEXT.md</file>
  <file>skills/sealos-deploy/SKILL.md</file>
  <file>skills/sealos-deploy/modules/pipeline.md</file>
  <file>skills/sealos-deploy/schemas/state.schema.json</file>
  <file>.planning/phases/09-service-and-adjacent-skill-entry-refactors/09-CONTEXT.md</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Make the composite deploy contract entry-visible</name>
  <files>skills/sealos-deploy/SKILL.md, skills/sealos-deploy/references/deploy-contract.md</files>
  <action>Keep preflight/auth/workspace and eligibility before artifact or provider work. Add a concise composite phase map, typed upstream envelope requirements, artifact ownership table, DEPLOY/UPDATE mode boundary, terminal success/stopped/error evidence, runtime-pending non-acceptance behavior, and main-versus-brain-deploy-preview boundary. Keep detailed mechanics one level deep and preserve existing helper ownership.</action>
  <verify><automated>rg -n "source|owner|precondition|evidence|redaction|success|stopped|error|DEPLOY|UPDATE|brain-deploy-preview" skills/sealos-deploy/SKILL.md skills/sealos-deploy/references/deploy-contract.md</automated></verify>
  <done>The entry names every phase, owner, artifact, terminal field, and downstream Canvas boundary needed by later plans.</done>
  <acceptance_criteria>The entry names every phase, owner, artifact, terminal field, and downstream Canvas boundary needed by later plans.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add a typed deploy handoff schema and offline gate</name>
  <files>skills/sealos-deploy/schemas/deploy-handoff.schema.json, scripts/test_deploy_entry_contract.mjs</files>
  <action>Create a schema for source, owner, preconditions, inputArtifact, allowedAction, failureReturn, responseOwner, evidence, redaction, terminalState, artifactPaths, and nextAction. Add synthetic positive/stopped/error traces, reject missing fields and unredacted values, and verify every referenced deploy contract link stays repository-local.</action>
  <verify><automated>node --check scripts/test_deploy_entry_contract.mjs && node scripts/test_deploy_entry_contract.mjs && python3 -m json.tool skills/sealos-deploy/schemas/deploy-handoff.schema.json >/dev/null && git diff --check</automated></verify>
  <done>The schema and test distinguish complete success from stopped/error traces and keep provider credentials out of fixtures.</done>
  <acceptance_criteria>The schema and test distinguish complete success from stopped/error traces and keep provider credentials out of fixtures.</acceptance_criteria>
</task>
</tasks>

<verification>
Run the entry contract test, schema JSON parsing, link checks, and `git diff --check`. Keep all fixtures synthetic and provider-free.
</verification>

<success_criteria>
Deploy has one entry-visible typed envelope contract with deterministic validation and no runtime behavior drift.
</success_criteria>
