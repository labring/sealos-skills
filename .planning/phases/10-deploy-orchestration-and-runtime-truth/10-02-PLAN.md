---
phase: 10-deploy-orchestration-and-runtime-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - skills/sealos-deploy/modules/pipeline.md
  - skills/sealos-deploy/scripts/validate-artifacts.mjs
  - skills/sealos-deploy/schemas/state.schema.json
  - scripts/test_deploy_pipeline_contract.mjs
  - tests/fixtures/deploy-pipeline-contract.json
autonomous: true
requirements:
  - SDS-D01
  - SDS-D03
must_haves:
  truths:
    - "Pipeline consumes readiness, Dockerfile, and template evidence with source, owner, preconditions, and verification before the next phase."
    - "Artifact readers validate schemas before resume or UPDATE decisions and keep lazy build artifacts absent when no build runs."
    - "State/live identity mismatches stop before mutation and verified last_deploy data is sufficient for the Canvas handoff."
  artifacts:
    - path: skills/sealos-deploy/modules/pipeline.md
      provides: "Typed dependency consumption, mode authority, and artifact lifecycle"
    - path: skills/sealos-deploy/scripts/validate-artifacts.mjs
      provides: "Deploy handoff and artifact validation before trust"
    - path: scripts/test_deploy_pipeline_contract.mjs
      provides: "Offline pipeline order, mismatch, and artifact tests"
    - path: tests/fixtures/deploy-pipeline-contract.json
      provides: "Synthetic positive/stopped/error pipeline traces"
  key_links:
    - from: skills/cloud-native-readiness/SKILL.md
      to: skills/sealos-deploy/modules/pipeline.md
      via: "Readiness report envelope consumed once before Docker packaging"
    - from: skills/dockerfile-skill/SKILL.md
      to: skills/sealos-deploy/modules/pipeline.md
      via: "Validated Dockerfile/build result handoff"
    - from: skills/docker-to-sealos/SKILL.md
      to: skills/sealos-deploy/modules/pipeline.md
      via: "Quality-gated template handoff"
    - from: skills/sealos-deploy/modules/pipeline.md
      to: skills/sealos-canvas/SKILL.md
      via: "Validated state and sanitized Runtime Truth tuple"
---

<objective>
Wire the dependency handoffs and artifact schemas into the existing deployment pipeline while preserving phase order and mode detection.

Purpose: Prevent repeated discovery and stale-state mutation across DEPLOY, UPDATE, and resume paths.
Output: Typed pipeline consumption rules, pre-trust artifact validation, and provider-free pipeline traces.
</objective>

<read_first>
  <file>.planning/phases/10-deploy-orchestration-and-runtime-truth/10-CONTEXT.md</file>
  <file>.planning/phases/10-deploy-orchestration-and-runtime-truth/10-01-PLAN.md</file>
  <file>skills/sealos-deploy/modules/pipeline.md</file>
  <file>skills/sealos-deploy/scripts/validate-artifacts.mjs</file>
  <file>skills/sealos-deploy/schemas/state.schema.json</file>
  <file>skills/cloud-native-readiness/SKILL.md</file>
  <file>skills/dockerfile-skill/SKILL.md</file>
  <file>skills/docker-to-sealos/SKILL.md</file>
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Consume typed dependency reports at pipeline boundaries</name>
  <files>skills/sealos-deploy/modules/pipeline.md</files>
  <action>Document and enforce source/owner/precondition/evidence/redaction checks for readiness, Dockerfile, and template inputs. Require the receiving phase to validate the incoming report and record a compact verified summary before advancing. Keep existing fast path, build reuse, `.sealos` artifact paths, and branch-specific dependency behavior intact.</action>
  <verify><automated>rg -n "inputArtifact|responseOwner|precondition|redaction|verification|quality gate|source" skills/sealos-deploy/modules/pipeline.md</automated></verify>
  <done>Every dependency transition has an explicit accepted, stopped, and failed return path with no second source scan.</done>
  <acceptance_criteria>Every dependency transition has an explicit accepted, stopped, and failed return path with no second source scan.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Gate resume and UPDATE trust on validated artifacts and live identity</name>
  <files>skills/sealos-deploy/scripts/validate-artifacts.mjs, skills/sealos-deploy/schemas/state.schema.json</files>
  <action>Extend artifact validation for the deploy handoff envelope, artifact provenance, redaction, and state/live identity fields. Make malformed or mismatched state stop before mutation, keep `build/` lazy when image reuse skips Phase 4, and preserve history contracts for deploy, set-image, env, patch, and rollback actions.</action>
  <verify><automated>node --check skills/sealos-deploy/scripts/validate-artifacts.mjs && node scripts/test_deploy_pipeline_contract.mjs --artifacts</automated></verify>
  <done>Valid artifacts pass, malformed/mismatched artifacts return actionable stopped/error diagnostics, and no secret values appear in validation output.</done>
  <acceptance_criteria>Valid artifacts pass, malformed/mismatched artifacts return actionable stopped/error diagnostics, and no secret values appear in validation output.</acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add deterministic pipeline traces</name>
  <files>scripts/test_deploy_pipeline_contract.mjs, tests/fixtures/deploy-pipeline-contract.json</files>
  <action>Create synthetic traces for typed dependency success, incomplete handoff, state/live mismatch, image-reuse no-build, and Canvas-ready state. Assert phase ordering, artifact paths, terminal states, and redaction. Keep all cluster/API interactions mocked or static.</action>
  <verify><automated>node --check scripts/test_deploy_pipeline_contract.mjs && node scripts/test_deploy_pipeline_contract.mjs && python3 -m json.tool tests/fixtures/deploy-pipeline-contract.json >/dev/null && git diff --check</automated></verify>
  <done>Pipeline traces prove evidence reuse and fail-closed state handling without provider access.</done>
  <acceptance_criteria>Pipeline traces prove evidence reuse and fail-closed state handling without provider access.</acceptance_criteria>
</task>
</tasks>

<verification>
Run pipeline contract tests, schema validation against valid and violating fixtures, existing artifact validator tests, and `git diff --check`.
</verification>

<success_criteria>
Deploy consumes typed dependency evidence once, validates artifacts before trust, and produces a verified state/Canvas handoff in both deploy and update paths.
</success_criteria>
