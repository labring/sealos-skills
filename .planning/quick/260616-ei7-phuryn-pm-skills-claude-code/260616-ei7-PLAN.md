---
quick_id: 260616-ei7
slug: phuryn-pm-skills-claude-code
mode: quick
type: execute
autonomous: true
owned_files:
  - README.md
  - distribution/platforms.json
  - scripts/validate-codex-plugin.py
  - .planning/quick/260616-ei7-phuryn-pm-skills-claude-code/260616-ei7-SUMMARY.md
verification:
  - python3 scripts/validate-codex-plugin.py
  - python3 -m json.tool distribution/platforms.json >/dev/null
  - python3 -m json.tool marketplace.json >/dev/null
  - python3 -m json.tool .claude-plugin/plugin.json >/dev/null
  - python3 -m json.tool .claude-plugin/marketplace.json >/dev/null
---

<objective>
Optimize the Claude Code install path by following the phuryn/pm-skills native Claude Code CLI marketplace pattern.

Purpose: Claude Code users should see `claude plugin marketplace add labring/sealos-skills` plus `claude plugin install sealos@sealos` as the primary install flow, with the existing `npx plugins add ... --target claude-code` path preserved as the compatibility fallback.

Output: README install copy, platform registry metadata, validator drift checks, and quick-task summary.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@README.md
@distribution/platforms.json
@marketplace.json
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@scripts/validate-codex-plugin.py
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Optimize Claude Code install docs using phuryn/pm-skills pattern. | Tasks 1, 2, 3 |
| REQ | Native Claude Code install must use `claude plugin marketplace add labring/sealos-skills` and `claude plugin install sealos@sealos`. | Tasks 1, 2, 3 |
| REQ | Existing npx Claude Code install path remains as compatibility fallback. | Tasks 1, 2, 3 |
| RESEARCH | Reference pattern maps phuryn/pm-skills `claude plugin install <plugin>@pm-skills` to Sealos `claude plugin install sealos@sealos`. | Tasks 1, 2, 3 |
| CONTEXT | Root `skills/**` stays canonical and deploy/runtime skill behavior remains stable. | Tasks 1, 2, 3 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Make native Claude Code install the README primary path</name>
  <files>README.md</files>
  <action>Update the `### Install in Claude Code` section so the first install block is exactly `claude plugin marketplace add labring/sealos-skills` followed by `claude plugin install sealos@sealos`. Keep `/sealos` as the invocation surface and keep the existing `npx plugins add https://github.com/labring/sealos-skills --target claude-code` command in a compatibility fallback paragraph after the native CLI block. Update the `Other supported AI tools` table so the Claude Code row presents the same native command pair as the install value and uses `/sealos` as usage. Preserve host-specific separation: Codex examples use `$sealos`, Claude Code examples use `/sealos`, direct skills.sh examples use `/sealos-*`.</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; text=Path('README.md').read_text(); section=text.split('### Install in Claude Code',1)[1].split('\n### ',1)[0]; native='claude plugin marketplace add labring/sealos-skills\nclaude plugin install sealos@sealos'; fallback='npx plugins add https://github.com/labring/sealos-skills --target claude-code'; native_pos=section.find(native); fallback_pos=section.find(fallback); assert native_pos != -1; assert fallback_pos != -1; assert [native_pos, fallback_pos] == sorted([native_pos, fallback_pos]); assert '/sealos deploy this repo to Sealos Cloud' in section"</automated>
  </verify>
  <done>README Claude Code section and tool table show native Claude Code CLI marketplace install as primary, npx as compatibility fallback, and `/sealos` as the invocation path.</done>
</task>

<task type="auto">
  <name>Task 2: Align Claude Code platform registry metadata</name>
  <files>distribution/platforms.json</files>
  <action>Update the `claude-code` platform entry to use `claude plugin marketplace add labring/sealos-skills && claude plugin install sealos@sealos` as `install`, move `npx plugins add https://github.com/labring/sealos-skills --target claude-code` to `alternateInstall`, keep `invoke` as `/sealos`, keep `runtime` as `plugin`, and keep `claim` scoped to `install-target`. Refresh `lastVerified` to `2026-06-16` for the docs/registry contract update. Expand evidence to include `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `marketplace.json`, and `commands/sealos.md` so the registry points at the Claude-compatible assets that define the install surface.</action>
  <verify>
    <automated>python3 -m json.tool distribution/platforms.json >/dev/null</automated>
    <automated>python3 -c "import json; p=json.load(open('distribution/platforms.json')); c=[x for x in p['platforms'] if x['id']=='claude-code'][0]; assert c['install']=='claude plugin marketplace add labring/sealos-skills && claude plugin install sealos@sealos'; assert c['alternateInstall']=='npx plugins add https://github.com/labring/sealos-skills --target claude-code'; assert c['invoke']=='/sealos'; assert c['claim']=='install-target'; assert c['lastVerified']=='2026-06-16'"</automated>
  </verify>
  <done>Platform registry has a Claude Code entry whose install, alternateInstall, invocation, evidence, and verification date match the new README contract.</done>
</task>

<task type="auto">
  <name>Task 3: Protect the Claude Code install contract in the validator</name>
  <files>scripts/validate-codex-plugin.py, .planning/quick/260616-ei7-phuryn-pm-skills-claude-code/260616-ei7-SUMMARY.md</files>
  <action>Extend `scripts/validate-codex-plugin.py` with Claude Code constants and checks alongside the existing Codex contract checks. Load `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`. Validate that README contains the exact native Claude Code command block, that README contains the exact npx fallback command, that `distribution/platforms.json` has one `claude-code` entry with the install and alternateInstall values from Tasks 1-2, and that the Claude-compatible marketplace/plugin metadata uses canonical plugin id `sealos`, repository `https://github.com/labring/sealos-skills`, source `./`, commands `./commands/`, and the same core skill paths already exposed by the root marketplace. Keep the validator name if desired, while the implementation should clearly cover both Codex and Claude Code distribution contracts. Run the verification commands, commit the source/docs changes with an English message, and create the summary file with status, files changed, verification results, and commit hash.</action>
  <verify>
    <automated>python3 scripts/validate-codex-plugin.py</automated>
    <automated>python3 -m json.tool marketplace.json >/dev/null</automated>
    <automated>python3 -m json.tool .claude-plugin/plugin.json >/dev/null</automated>
    <automated>python3 -m json.tool .claude-plugin/marketplace.json >/dev/null</automated>
    <automated>rg -n "claude plugin marketplace add labring/sealos-skills|claude plugin install sealos@sealos|npx plugins add https://github.com/labring/sealos-skills --target claude-code" README.md distribution/platforms.json scripts/validate-codex-plugin.py</automated>
  </verify>
  <done>Validator fails on drift from the native Claude Code install contract, all JSON syntax checks pass, summary exists at `.planning/quick/260616-ei7-phuryn-pm-skills-claude-code/260616-ei7-SUMMARY.md`, and the quick-task commit records the completed changes.</done>
</task>

</tasks>

<verification>
Run the task-level automated checks, then run:

- `python3 scripts/validate-codex-plugin.py`
- `python3 -m json.tool distribution/platforms.json >/dev/null`
- `python3 -m json.tool marketplace.json >/dev/null`
- `python3 -m json.tool .claude-plugin/plugin.json >/dev/null`
- `python3 -m json.tool .claude-plugin/marketplace.json >/dev/null`
</verification>

<success_criteria>
- README presents native Claude Code marketplace CLI install as the primary Claude Code path.
- README and `distribution/platforms.json` both preserve the npx Claude Code command as compatibility fallback.
- `scripts/validate-codex-plugin.py` enforces the README and platform registry values for Claude Code.
- `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` remain aligned with root marketplace identity and skill exposure.
- `.planning/quick/260616-ei7-phuryn-pm-skills-claude-code/260616-ei7-SUMMARY.md` records the completed work, verification results, and commit hash.
</success_criteria>
