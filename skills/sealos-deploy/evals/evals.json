{
  "skill_name": "sealos-deploy",
  "evals": [
    {
      "id": 0,
      "prompt": "/sealos-deploy https://github.com/example/low-score-monorepo 。根目录是工具包，但 packages/docs 是一个可以构建的文档站点。",
      "expected_output": "Runs the opening impossibility judgment, treats the repository as a source boundary, warns on a low score without stopping, and selects the project-backed docs application.",
      "files": [],
      "assertions": [
        {
          "name": "low-score-continues",
          "description": "A score below 4 produces a warning and does not itself stop the pipeline."
        },
        {
          "name": "finds-child-application",
          "description": "Inspects child applications and selects the repository-backed docs site instead of assuming the root is the only deployable unit."
        },
        {
          "name": "writes-current-analysis",
          "description": "Writes schema-valid analysis.json with honest open-vocabulary facts and no separate entry-classifier artifact."
        }
      ]
    },
    {
      "id": 1,
      "prompt": "/sealos-deploy https://github.com/example/exact-official-app 。官方 catalog 中只有一个 spec.gitRepo 精确匹配。",
      "expected_output": "Selects the verified exact official Template solely from the GitHub repository match, keeps the materialized official YAML as immutable provenance, permits only schema-driven field repairs in the delivery copy, preserves every resource identity, writes empty official-route aggregate build artifacts, and finishes.",
      "files": [],
      "assertions": [
        {
          "name": "verified-exact-only",
          "description": "Fast path requires one exact spec.gitRepo match from a refreshed verified official catalog."
        },
        {
          "name": "repository-identity-only",
          "description": "The normalized GitHub owner/repo is the only project-side input to the exact official-template reuse decision."
        },
        {
          "name": "preserves-official-provenance",
          "description": "Keeps the materialized official reference unchanged while allowing only schema-driven field repairs that preserve the complete resource set."
        },
        {
          "name": "stable-official-artifacts",
          "description": "Writes route=official-template build-request.json with no services or primary service and a skipped aggregate build-result.json with a null Brain projection."
        },
        {
          "name": "prepare-only-finish",
          "description": "Validates the handoff and stops without cloud deployment or runtime verification."
        }
      ]
    },
    {
      "id": 2,
      "prompt": "/sealos-deploy https://github.com/example/exact-official-app/tree/feature 。官方 catalog 有唯一精确匹配；sandbox 中还有本地文件。",
      "expected_output": "Normalizes the URL to example/exact-official-app and selects the unique verified official Template regardless of branch or sandbox checkout state.",
      "files": [],
      "assertions": [
        {
          "name": "ignores-worktree-state",
          "description": "Branch, checkout cleanliness, local files, and selected subdirectories do not gate a repository-identity match."
        },
        {
          "name": "decision-driven-route",
          "description": "Reads template-references.json decision.route instead of inferring the route from file existence."
        },
        {
          "name": "no-similar-reference-flow",
          "description": "Does not select or consume a structurally similar catalog template."
        }
      ]
    },
    {
      "id": 3,
      "prompt": "/sealos-deploy 一个 Compose 项目：frontend 有可验证的公开 digest，api 只有 build，postgres 要转换为 KubeBlocks，worker 也只有 build。",
      "expected_output": "Preserves the full topology, creates one aggregate standard build request for frontend/api/worker but not the managed database container, reuses frontend, builds api and worker independently, and aggregates results.",
      "files": [],
      "assertions": [
        {
          "name": "complete-topology",
          "description": "Retains frontend, API, database capability, and worker rather than stopping after the first image."
        },
        {
          "name": "aggregate-request",
          "description": "Writes one version 2.0 build-request.json containing every final container service with unique artifact keys and an explicit primary_service."
        },
        {
          "name": "managed-db-not-built",
          "description": "Keeps PostgreSQL topology for KubeBlocks conversion without building the source database container."
        },
        {
          "name": "aggregate-result",
          "description": "Records reused and independently built services in one completed build-result.json and projects the requested primary service to Brain's top-level fields."
        }
      ]
    },
    {
      "id": 4,
      "prompt": "/sealos-deploy https://github.com/example/private-source-app 。没有 Docker daemon，需要构建镜像；环境已经注入 GITHUB_TOKEN、kubeconfig 和 VersityGW 配置。",
      "expected_output": "Uses the injected token and current sandbox Kubernetes identity, packages the local context to VersityGW, builds with Kaniko, pushes GHCR, and records the immutable digest.",
      "files": [],
      "assertions": [
        {
          "name": "no-browser-auth",
          "description": "Does not start browser authentication or ask the user to log in to GitHub or Sealos."
        },
        {
          "name": "current-sandbox-identity",
          "description": "Uses the current namespace, kubeconfig, and service account without region/workspace selection or admin fallback."
        },
        {
          "name": "kaniko-build",
          "description": "Uses the k8s-kaniko-job path and never requires a Docker daemon."
        },
        {
          "name": "immutable-result",
          "description": "Extracts Kaniko's digest, records a linux/amd64 immutable image ref, and exposes the primary result through top-level image and Kubernetes fields."
        }
      ]
    },
    {
      "id": 5,
      "prompt": "/sealos-deploy 一个多服务项目。api 的 Kaniko 构建因 Dockerfile COPY 路径错误失败，web 已经成功。",
      "expected_output": "Classifies an api build-plan failure, minimally repairs only api through Phase 3, retries it within the per-service limit, and does not rebuild web.",
      "files": [],
      "assertions": [
        {
          "name": "service-scoped-repair",
          "description": "Routes only the failed api service through the Phase 4 to Phase 3 repair loop."
        },
        {
          "name": "bounded-retries",
          "description": "Limits build-plan repairs to three attempts for the failing service."
        },
        {
          "name": "preserves-successes",
          "description": "Does not rerun or rewrite services that already succeeded."
        },
        {
          "name": "failure-has-no-image",
          "description": "A failed result does not expose a deployable digest."
        }
      ]
    },
    {
      "id": 6,
      "prompt": "/sealos-deploy 一个需要构建的应用。推送到 GHCR 后匿名 pull 返回 403。",
      "expected_output": "Keeps the digest, records ghcr_secret_required, adds only an app-scoped imagePullSecrets reference to the affected workload, and leaves credential materialization downstream.",
      "files": [],
      "assertions": [
        {
          "name": "records-private-pull",
          "description": "Records ghcr_secret_required while retaining the immutable built image."
        },
        {
          "name": "reference-only",
          "description": "Template contains the affected workload imagePullSecrets reference using defaults.app_name."
        },
        {
          "name": "no-inline-credentials",
          "description": "Template and delivery artifacts contain no Docker config, registry Secret payload, token, username, or password."
        }
      ]
    },
    {
      "id": 7,
      "prompt": "/sealos-deploy 一个应用需要 ADMIN_PASSWORD、SMTP_PASSWORD 和可选 LOG_LEVEL。",
      "expected_output": "Classifies configuration accurately, retains required/optional Template inputs, validates string defaults, and does not collect or resolve values in the prepare workflow.",
      "files": [],
      "assertions": [
        {
          "name": "classifies-env-vars",
          "description": "Records required, auto, and optional environment variable categories from project evidence."
        },
        {
          "name": "keeps-template-inputs",
          "description": "Preserves required and optional values as Template inputs/defaults for the downstream system."
        },
        {
          "name": "no-interactive-config",
          "description": "Does not run the user-facing interactive configuration phase."
        }
      ]
    },
    {
      "id": 8,
      "prompt": "/sealos-deploy 一个 Helm 项目，包含 Deployment、worker、Service、Ingress、ConfigMap、RBAC 和一个受支持数据库。",
      "expected_output": "Renders Helm in a temporary copy, preserves resource/container identity, uses the Kubernetes adapter directly, accounts for every source resource, and validates topology evidence.",
      "files": [],
      "assertions": [
        {
          "name": "safe-helm-render",
          "description": "Uses Helm 3 in a temporary chart copy with no helm install and no source mutation."
        },
        {
          "name": "kubernetes-adapter",
          "description": "Uses kubernetes_to_template.py on rendered.yaml rather than collapsing topology into Compose."
        },
        {
          "name": "resource-accounting",
          "description": "Writes resource-map.json and validator-only topology evidence with no silently dropped resources."
        },
        {
          "name": "database-semantics",
          "description": "Uses KubeBlocks only if initialization and runtime semantics are preserved, otherwise retains an annotated raw fallback."
        }
      ]
    },
    {
      "id": 9,
      "prompt": "/sealos-deploy 完成镜像和模板准备后，请直接把它部署到云端并确认 URL。",
      "expected_output": "Finishes locally and target-cluster validated Brain delivery artifacts but explicitly stops before deployment because downstream owns deployment and runtime feedback.",
      "files": [],
      "assertions": [
        {
          "name": "complete-delivery",
          "description": "Writes analysis, template references, aggregate build request/result, Template YAML, and delivery manifest."
        },
        {
          "name": "cross-validates",
          "description": "Requires aligned routes, services, primary Brain projection, immutable images, pull-Secret references, existing manifest paths, a passing local YAML quality gate, and a passing target-cluster server-side dry-run."
        },
        {
          "name": "does-not-deploy",
          "description": "Does not call Sealos auth, choose region/workspace, invoke the Template API, persist final YAML, or write deployment state; the required server-side dry-run remains non-persistent."
        },
        {
          "name": "honest-final-status",
          "description": "Reports preparation complete and does not claim that the application is running or has a verified URL."
        }
      ]
    },
    {
      "id": 10,
      "prompt": "/sealos-deploy 一个模板已通过本地质量门，但目标集群对其中一个 KubeBlocks Cluster 返回 strict decoding unknown field。",
      "expected_output": "Keeps the delivery Template unresolved, privately renders validation scenarios, dry-runs each runtime document separately, repairs only the schema-invalid field in the canonical YAML, reruns the full local and target gates, and writes the delivery manifest after no blocking failure remains.",
      "files": [],
      "assertions": [
        {
          "name": "private-rendered-copy",
          "description": "Never sends the unresolved Template or kind Template metadata to kubectl and never overwrites the delivery YAML with rendered values."
        },
        {
          "name": "per-document-strict-server-gate",
          "description": "Runs kubectl apply --dry-run=server --validate=strict for each target runtime document using the current context and namespace."
        },
        {
          "name": "complete-repair-loop",
          "description": "Uses only category=schema with repairable=true to edit YAML, reruns the complete local quality gate, rerenders all scenarios, and rechecks every document."
        },
        {
          "name": "manifest-after-target-pass",
          "description": "Does not write or confirm delivery-manifest.json before the complete target-cluster gate passes and never performs a persistent apply."
        }
      ]
    },
    {
      "id": 11,
      "prompt": "/sealos-deploy 官方 Matomo 模板。Cluster 的 noCreatePDB 被目标 Schema 拒绝，同时 Role 和 RoleBinding 因 sandbox 身份缺少 bind/escalate 权限而返回授权错误。",
      "expected_output": "Removes only the schema-invalid noCreatePDB field, records RBAC authorization results as non-blocking warnings, preserves the ServiceAccount, Role, RoleBinding, Cluster, Job, and every other official resource, and completes the prepare handoff.",
      "files": [],
      "assertions": [
        {
          "name": "schema-only-repair",
          "description": "Only the unknown noCreatePDB field is eligible for automatic YAML repair, backed by a target-gate authorization scoped to the original official Template digest."
        },
        {
          "name": "authorization-is-warning",
          "description": "Forbidden, bind, and escalate results are warnings caused by the sandbox identity and never authorize YAML changes."
        },
        {
          "name": "official-resource-set-invariant",
          "description": "The validator rejects any official delivery copy that adds, removes, or renames a resource, or changes images, digests, or resource quotas outside a server-authorized Schema field."
        }
      ]
    },
    {
      "id": 12,
      "prompt": "/sealos-deploy 官方 Mastodon 模板。它在 env 数组中使用 ${{ if(...) }} 条件块，并用 random(128).toLowerCase().replace(/[^0-9a-f]/g, 'a') 生成密钥。",
      "expected_output": "Keeps the official Template unresolved for handoff, privately expands every reachable conditional-array scenario before YAML parsing and target dry-run, supports the template's bounded random expression, and never changes its image digest or resource quotas without a Schema rejection.",
      "files": [],
      "assertions": [
        {
          "name": "conditional-array-render-before-parse",
          "description": "Removes Sealos if/elif/else/endif control lines for each private scenario before parsing YAML arrays, so collection keys are never stringified."
        },
        {
          "name": "mastodon-random-expression",
          "description": "Safely renders the official 128-character lowercase hexadecimal random expression without raw eval."
        },
        {
          "name": "unresolved-delivery-preserved",
          "description": "Leaves the official delivery Template unresolved and rejects image-digest or resource-quota drift that lacks server-side Schema authorization."
        }
      ]
    }
  ]
}
